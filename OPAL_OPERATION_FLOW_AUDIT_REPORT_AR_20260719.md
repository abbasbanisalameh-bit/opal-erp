# تقرير تدقيق سير العمليات والتكامل في OPAL ERP

**الإصدار المعتمد قبل التدقيق:** `OPAL_SATISFACTION_ANALYTICS_UI_POLISH_V1_20260719`  
**الإصدار الناتج:** `OPAL_OPERATION_FLOW_UNIFICATION_V1_20260719`  
**تاريخ التدقيق:** 2026-07-19

## 1. حدود التدقيق

تم تدقيق **كود النسخة التي ثبّتها المستخدم**، ومسارات Django، والنماذج والخدمات والقوالب والصلاحيات والاختبارات. لم تُرفق قاعدة بيانات الموقع الفعلية، لذلك لا يمكن الجزم بعدد التعارضات الموجودة في بيانات الإنتاج. لهذا أضيف إلى النظام فحص حي يقرأ قاعدة البيانات بعد تركيب التحديث ويعرض النتيجة للمدير دون تعديل أو حذف أي سجل.

شمل التدقيق:

- 283 مسارًا مسجلًا في Django.
- جميع تطبيقات الطلاب، التسجيل، الأكاديميات، المعلمين، الجدول، الحضور، الامتحانات، الرسوم، أولياء الأمور، الوثائق، التواصل، التقارير، OpenEMIS، ومركز التطوير.
- 160 اختبارًا آليًا فريدًا.
- مقارنة الشاشات التي تنشئ أو تعدل البيانات مع مصادر الحقيقة الرسمية.

## 2. النتيجة التنفيذية

بنية OPAL قوية وقابلة للوصول إلى تشغيل مدرسي متكامل، وأهم عناصرها الصحيحة موجودة فعلًا: نموذج طالب واحد، قيد أكاديمي رسمي، ملف أسرة موحد، تكليف معلم رسمي، جدول مرتبط بالتكليف، رسوم مدرسية مبسطة، بوابتا المعلم وولي الأمر، وثائق، تواصل، صلاحيات وسجل عمليات.

المشكلة الرئيسية لم تكن غياب الوحدات، بل **توزع المداخل، وجود روابط تاريخية، وبعض الروابط التي كانت تحوّل ميزة قائمة إلى شاشة أخرى**. عالج التحديث ذلك بدليل عمليات موحد، وبحث علوي فعلي، ومسارات تشغيل من البداية إلى النهاية، وفحص تكامل حي.

لم يُحذف أي نموذج أو مهمة أو ميزة أو جدول، ولم تُخفَ ميزة بغرض التبسيط. الميزات الموجودة التي كانت معطلة بالتحويل، مثل الخطة الدراسية وفئات الرسوم ورسوم الطلاب والأقساط والخصومات، أصبحت ظاهرة في مواقعها الرسمية.

## 3. سير العمليات الرسمي بعد التحديث

### 3.1 تهيئة العام الدراسي

إعدادات المدرسة ← الفروع ← العام الدراسي الحالي ← الفصل الحالي ← الصفوف والشعب ← المواد ← الخطة الدراسية.

**قاعدة التكامل:** لا يبدأ التسجيل أو الجدول أو الحضور أو الامتحانات قبل تحديد عام وفصل حاليين وبناء الهيكل الدراسي.

### 3.2 المعلم والجدول

ملف المعلم وحسابه ← التكليف بالمادة والشعبة ← إعداد أوقات الحصص ← منشئ الجدول الذكي ← الجدول النهائي ← بوابة المعلم.

**مصدر الحقيقة:** `teachers.TeacherAssignment` هو الرابط الرسمي بين المعلم والعام والشعبة والمادة. لا تُنشأ حصة لمعلم خارج تكليفه.

### 3.3 التسجيل والطالب والأسرة

مرشح للقبول اختياريًا ← التسجيل الذكي ← `students.Student` ← `academics.Enrollment` ← ملف الأسرة وحساب ولي الأمر ← الرسم والدفعة الأولى والإيصال ← بطاقة الطالب 360.

**مصدر الحقيقة:** `students.Student` هو نموذج الطالب الوحيد، و`Enrollment` هو مصدر الصف والشعبة والحالة الأكاديمية.

### 3.4 الرسوم المدرسية

فئات الرسوم ← رسوم الطلاب ← التسديد الموحد لطالب أو لجميع الإخوة ← التوزيع الآلي ← الإيصال ← أرشيف التسديد ← الأقساط والخصومات ← كشف 28 الشهري ← الإغلاق المالي.

**قاعدة التكامل:** إدخال الدفعات يبقى من شاشة التسديد الموحدة فقط. بقية شاشات الرسوم للتهيئة والمتابعة والقرارات، وليست مدخل دفع بديلًا.

### 3.5 التشغيل اليومي والنتائج

الحضور ← الواجبات ← الامتحانات ← إدخال العلامات من تكليف المعلم ← مراجعة الإدارة واعتمادها ونشرها ← عرضها في بوابة ولي الأمر ← تحليل النتائج.

### 3.6 التواصل والرقابة

شكوى أو اقتراح + تقييم جودة التدريس + تقييم الخدمات الإلكترونية ← مراجعة الإدارة والرد ← تعميم أو تنبيه أو إعلان ← إشعار المستخدم ← التقارير وسجل العمليات.

### 3.7 نهاية العام

اكتمال النتائج ← معالجة المتبقي المالي ← الإغلاق المالي ← الإغلاق الأكاديمي ← الترفيع أو التخرج ← تهيئة العام الجديد.

## 4. الأخطاء ومواطن الضعف المكتشفة ومعالجتها

### 4.1 جدول المعلم لم يكن يظهر داخل ملفه

كان ملف المعلم يستخدم علاقة Django غير صحيحة `timetableentry_set` رغم أن النموذج يعرّف العلاقة باسم `timetable_entries`، ثم كان يخفي الخطأ بواسطة `except Exception`. النتيجة أن جدول المعلم يظهر فارغًا بصمت.

**الحل:** استخدام العلاقة الصحيحة وإزالة إخفاء الاستثناء. أضيف اختبار وظيفي يثبت ظهور المادة والحصة في ملف المعلم.

### 4.2 عداد المعلمين في لوحة الإدارة

كان العداد يعتمد على جميع مستخدمي `is_staff`، فيخلط المدير والموظف بالمعلم.

**الحل:** الاعتماد على سجلات `Teacher` النشطة فقط.

### 4.3 مؤشر الحضور اليومي

كانت لوحة الإدارة تطلب حالة تاريخية باسم `excused` بينما نموذج الحضور الحالي يستخدم `departed` للمغادرة.

**الحل:** توحيد الاستعلام والعرض والمخطط على الحالة الفعلية «مغادر».

### 4.4 بحث الطلاب مع مرشح الحالة

كان البحث النصي يعيد بناء الاستعلام بطريقة قد تفقد مرشح الحالة.

**الحل:** تطبيق البحث بواسطة `Q` ثم تطبيق مرشح الحالة على الاستعلام نفسه، مع اختبار طالب نشط وآخر مؤرشف.

### 4.5 البحث العلوي كان شكليًا

حقل البحث في الشريط العلوي لم يكن ينفذ بحثًا وظيفيًا في عمليات النظام.

**الحل:** تحويله إلى بحث سريع حسب الدور في دليل العمليات، مع `Ctrl/Command + K` والتنقل بلوحة المفاتيح.

### 4.6 ميزات موجودة لكنها محولة إلى شاشة أخرى

كانت الخطة الدراسية، وفئات الرسوم، ورسوم الطلاب، والأقساط، والخصومات تملك نماذج وخدمات فعلية، لكن عناوين URL كانت تحوّل المستخدم إلى شاشة أخرى، ما جعل الميزة مخفية عمليًا.

**الحل:** إعادة ربط كل ميزة بشاشتها الرسمية، وإنشاء القوالب الناقصة، وإضافتها إلى دليل العمليات والقوائم الفرعية. بقي إدخال الدفعات فقط في شاشة التسديد الموحدة لمنع التكرار.

### 4.7 صلاحيات بعض مسارات الرسوم والوثائق

بعض نقاط إدخال الرسوم والبحث والمعاينة والأرشيف، وقائمة الوثائق وتفاصيلها، كانت تعتمد على تسجيل الدخول دون توحيدها مع صلاحية الإدارة.

**الحل:** تطبيق حماية الإدارة على المسارات الإدارية، مع إبقاء الوثائق المسموحة لولي الأمر داخل بوابته الخاصة والتحقق العام من الوثيقة في رابط التحقق فقط.

### 4.8 تحويل ثابت في التقرير المالي الشهري

كان الرجوع بعد الحفظ يستخدم مسارًا نصيًا ثابتًا.

**الحل:** استخدام `reverse()` للمسار المسمى حتى لا ينكسر عند تغيير بنية URL.

### 4.9 تعارض بين الوثائق والإعدادات

كانت تعليقات الإعدادات وREADME تقول إن OpenEMIS ومركز التطوير مخفيان افتراضيًا، بينما القيم الفعلية مفعلة افتراضيًا.

**الحل:** توحيد التوثيق مع السلوك الفعلي، مع التأكيد أن OpenEMIS أساس فقط حتى توفر API رسمي.

## 5. التكامل الذي أضيف

- **دليل عمليات موحد حسب الدور** يحدد شاشة واحدة لكل عملية.
- **بحث عالمي فعلي** في العمليات من الشريط العلوي.
- **شارة المكان الرسمي للعملية** داخل الصفحات المعروفة في الدليل.
- **ست دورات تشغيل مترابطة** داخل دليل العمليات، من التهيئة حتى الرقابة.
- **19 فحص تكامل حي** لقاعدة البيانات، تشمل العام والفصل، الطالب والقيد والأسرة، الحسابات، التكليف والخطة والجدول، الحضور، الرسوم، الامتحانات والعلامات.
- أمر طرفية للرقابة:

```bash
python manage.py audit_operation_flow --allow-issues
python manage.py audit_operation_flow --json --allow-issues
```

- معيار موثق داخل `docs/OPAL_OPERATION_FLOW_STANDARD_AR.md`.
- اختبارات جديدة لتوحيد العمليات، الصلاحيات، البحث، جدول المعلم، وبقاء الميزات ظاهرة.

## 6. نقاط القوة

1. **مصدر طالب واحد** وعدم استخدام سجل أكاديمي بديل كنموذج طالب.
2. **بطاقة الطالب 360** تجمع القيد والأسرة والرسوم والحضور والعلامات والوثائق والجدول.
3. **التسجيل الذكي مترابط** وينشئ الطالب والقيد والأسرة والحساب والرسم والدفعة والإيصال ضمن مسار واحد.
4. **نموذج أسرة موحد** يربط الإخوة بحساب ولي أمر واحد.
5. **تكليف المعلم مصدر رسمي** تستخدمه العلامات والحضور والجدول والواجبات.
6. **منع تعارضات الجدول** للمعلم والشعبة والغرفة.
7. **مالية مدرسية مبسطة** تركز على الرسوم والخصومات والدفعات والإيصالات والمتبقي والأقساط والكشوف، دون توسيعها إلى محاسبة عامة.
8. **صلاحيات وبوابات منفصلة** للمدير والمعلم وولي الأمر.
9. **سجل عمليات ومركز سلامة بيانات ومركز تحديثات** تدعم التشغيل الآمن.
10. **تغطية اختبارية واسعة** لمسارات العمل الأساسية.

## 7. نقاط الضعف المتبقية وحلولها

### 7.1 جودة بيانات الموقع الفعلية غير معروفة في حزمة الكود

**الحل:** بعد التركيب افتح دليل العمليات وشغّل فحص التكامل على قاعدة الموقع، ثم عالج كل صف غير سليم حتى 100% قبل إدخال البيانات الحقيقية.

### 7.2 SQLite مناسبة للمرحلة الحالية وليست الخيار النهائي للتوسع

قد تصبح الكتابة المتزامنة والنسخ الاحتياطي والتقارير الثقيلة نقطة اختناق مع زيادة الفروع والمستخدمين.

**الحل:** تجهيز انتقال مخطط ومختبر إلى PostgreSQL قبل التشغيل متعدد الفروع أو التوسع الكبير.

### 7.3 OpenEMIS ما زال أساسًا تقنيًا

لا يمكن ضمان تكامل وزاري كامل دون مواصفات API وحقول واعتمادات رسمية.

**الحل:** عدم بناء افتراضات إضافية، وتجربة الربط في بيئة منفصلة فور استلام المواصفات الرسمية.

### 7.4 الاختبار الكامل في أمر واحد تجاوز مهلة بيئة البناء

لم يظهر فشل، لكن الأمر الأحادي استغرق أكثر من مهلة الأداة. جميع الاختبارات الفريدة البالغ عددها 160 نجحت عند تقسيمها إلى أربع مجموعات مستقلة.

**الحل:** اعتماد تشغيل الاختبارات في CI على مجموعات التطبيقات، ثم تشغيل المجموعة الكاملة في بيئة زمنها أطول قبل كل إصدار إنتاجي.

### 7.5 الروابط التاريخية تزيد حجم الصيانة

هي مفيدة للتوافق، لكنها تجعل عدد المسارات كبيرًا.

**الحل:** إبقاؤها كتحويلات فقط، ومنع إضافة أي منطق حفظ جديد إليها، ثم قياس استخدامها قبل حذفها في إصدار رئيسي مستقبلي وبعد موافقة صريحة؛ هذا التحديث لم يحذفها.

## 8. ما يلزم لاكتمال التكامل الأساسي

الأولوية ليست إضافة وحدات جديدة الآن، بل إغلاق الفجوات التالية ضمن النطاق المجمد:

1. تركيب التحديث وتشغيل فحص التكامل على بيانات الموقع ومعالجة النتيجة حتى 100% أو توثيق الاستثناءات.
2. تنفيذ اختبار قبول واقعي بأدوار: مدير عام، مدير/سكرتير، مسؤول رسوم، معلم، ولي أمر.
3. تجربة دورة كاملة: تهيئة عام ← تسجيل ← دفع ← حضور ← علامات ← نشر ← إغلاق ← ترفيع.
4. تشغيل الاختبارات في خط نشر مقسم وإلزام `check` و`migrate` و`collectstatic` وفحص التكامل قبل النشر.
5. التأكد من تغطية كل إجراء حساس في سجل العمليات، خصوصًا الإلغاء والحذف الآمن والاعتماد والنشر والإغلاق.
6. نقل قاعدة البيانات إلى PostgreSQL قبل التوسع التشغيلي الكبير.
7. إكمال OpenEMIS بعد وصول API الرسمي فقط.
8. بعد اعتماد الوظائف الأساسية بنسبة 100%، تبدأ مرحلة مستقلة للإضافات التوسعية والجماليات، دون خلطها بمرحلة الإكمال الحالية.

## 9. قرار الاعتماد

يمكن اعتماد هذا الإصدار **كنقطة أساس كودية جديدة** بعد نجاح التركيب على الموقع. الاعتماد التشغيلي النهائي للبيانات يظل مشروطًا بنتيجة فحص التكامل الحي وتجربة القبول بالأدوار الحقيقية.
