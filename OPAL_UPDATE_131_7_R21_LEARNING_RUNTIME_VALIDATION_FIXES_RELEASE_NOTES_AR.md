# ملاحظات إصدار OPAL Update 131.7 R21

## Learning Platform Runtime Validation Fixes

### إصلاحات الاعتماد

- أزيل اعتماد اختبار مساعد المتعلم على نص ترجمة Django المتغير بين الإصدارات.
- أصبح الاختبار يثبت أن الدورة غير موجودة ضمن queryset المسموح وأن النموذج أعاد خطأ للحقل ولم يُنشئ تفاعل ذكاء اصطناعي.
- صُحح عقد API ليتحقق من `request.META["HTTP_AUTHORIZATION"]` وصيغة Bearer المستخدمة فعليًا.
- صُحح محرك مركز التحديثات لنسخ `OPAL_UPDATE_MANIFEST.json` بدل استبعاده.
- أضيفت هجرة إصلاحية تعيد manifest في النشر الحالي المبني على R20.

### الأثر

- لا تغيير في نماذج البيانات التشغيلية.
- لا حذف أو تعديل لحسابات المنصة أو الدورات أو التقدم أو الاشتراكات.
- لا تغيير في صلاحيات المدير أو المدرس أو المتعلم.
- الإصدار مخصص لإغلاق فشل بوابة الاختبار وتثبيت هوية النشر.
