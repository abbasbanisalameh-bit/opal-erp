# تقرير التحقق — OPAL Update 131.5 R2

## النتيجة

الحزمة اجتازت بوابات التحقق الساكنة الخاصة بالمصدر وبمصمم لوحة المدير المبسّط.

## الفحوص المنفذة

- بوابة المصدر `tools/validate_opal_update_131_source.py`: **141/141 ناجحة**، دون أخطاء أو تحذيرات.
- تحليل تركيب Python: **566 ملفًا** دون خطأ نحوي.
- فحص JavaScript بواسطة `node --check`: ناجح.
- اختبارات العقود المتاحة دون بيئة Django: **43/43 ناجحة**.
- اختبار عقد Update 131.5 R2 يتضمن:
  - تخزين التفضيلات في `UserProfile` دون نموذج موازٍ.
  - مخطط تخطيط رقم 2 يعيد آثار الواجهة القديمة إلى الافتراضي.
  - تغطية ما لا يقل عن 25 عنصرًا؛ القالب الحالي يغطي 29 عنصرًا.
  - تحديد عنصر واحد أو عدة عناصر أو الجميع.
  - أداة مركزية واحدة وتخطيطات جاهزة.
  - السحب الجماعي، العرض، الارتفاع، الشكل، الحواف، الإظهار والإخفاء.
  - منع عودة قوائم التحكم المتكررة داخل كل عنصر.
  - عدم استخدام `localStorage` أو `sessionStorage` مصدرًا للحقيقة.

## التحقق الأمني

- الخادم يعيد بناء JSON من قائمة سماح؛ لا يحفظ حقولًا غير معروفة.
- لا يسمح بـCSS أو HTML حر.
- معرفات العناصر والأحجام والأنماط والحواف محددة مسبقًا.
- طلب التخطيط محدود إلى 48,000 بايت.
- الحزمة Code-only ولا تتضمن قاعدة بيانات أو أسرارًا أو وسائط أو بيئة افتراضية أو `staticfiles` مجمعة.
- هجرة `accounts.0002` تتحقق من وجود العمود قبل إضافته لتفادي تعارض استعادة كود جزئية.

## ما لم يُنفذ في بيئة البناء

بيئة البناء لا تحتوي Django ولا يمكنها تنزيل الحزم من الإنترنت، لذلك لم يُنفذ هنا:

```text
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py migrate
python manage.py collectstatic --noinput
```

هذه الأوامر إلزامية على PythonAnywhere وينفذها مركز التحديثات أو تُنفذ يدويًا بعد التركيب كما هو موضح في دليل التثبيت.

## الحزمة النهائية

- عدد الملفات داخل ZIP: **1046 ملفًا**.
- بصمة SHA-256 تُسلّم في ملف مستقل بجانب الحزمة لتجنب تضمين بصمة دورية داخل الأرشيف نفسه.
