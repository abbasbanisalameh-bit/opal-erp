# تعليمات تطبيق OPAL Template Consolidation Phase 1

يُطبّق هذا الإصدار من خلال **مركز تحديثات النظام** داخل OPAL فقط، ولا
يُنسخ يدويًا إلى PythonAnywhere.

1. افتح: الإعدادات ← مركز تحديثات النظام ← النسخ الموجودة على النظام.
2. احفظ نسخة إضافية من النظام الحالي باسم واضح (اختياري، لأن المركز ينشئ
   نسخة أمان تلقائيًا قبل الاستعادة).
3. ارفع ملف ZIP من بطاقة «رفع نسخة أو تحديث».
4. بعد ظهور الحزمة بحالة «صالحة»، اضغط «استعادة» واكتب كلمة `استعادة` للتأكيد.
5. ينتج المركز تلقائيًا نسخة أمان، ثم يشغّل `check` و`migrate` و`collectstatic`.
6. بعد ظهور رسالة النجاح، اضغط Reload من صفحة Web في PythonAnywhere، ثم اختبر
   التسجيل الذكي وStudent 360 والهيكل الأكاديمي والعلامات.
7. من تبويب GitHub في المركز، ارفع النظام الحالي بعد التأكد من عمله.

لا توجد ترحيلات جديدة متوقعة في هذا الإصدار.
