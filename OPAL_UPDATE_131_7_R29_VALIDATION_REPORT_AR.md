# تقرير تحقق OPAL Update 131.7 R29

## تحقق تم في بيئة البناء

- بوابة فحص المصدر الرسمية: **355/355 ناجحة**.
- تحليل AST: **646 ملف Python** بدون خطأ نحوي.
- `compileall`: ناجح.
- اختبارات العقود التي لا تتطلب Django: **13/13 ناجحة**:
  - عقد السعة/التوقيت: 6 اختبارات.
  - عقد R29 للبيانات المترابطة: 5 اختبارات.
  - عقد سياسة إيصالات ولي الأمر: اختباران.
- عقد السعة: 500 طالب / 200 ولي أمر / 30 معلم.
- عقد الأسر: 50×1 + 50×2 + 50×3 + 50×4 = 500 طالب، مع 200 اسم ولي أمر مميز و500 اسم طالب مميز وسلسلة أب/جد/عائلة متطابقة داخل كل أسرة.
- عقد الخطة: 30 حصة لكل شعبة؛ عربي 5، رياضيات 5، علوم 5، إنجليزي 5، إسلامية 2، مهني 2، رياضة 2، حاسوب 1، فنية 1، اجتماعيات 1، ثقافة مالية 1.
- عقد السعة الرياضية: 30 شعبة × 30 حصة = 900 = 30 معلم × 30 = 30 معلم × 6 × 5 أيام.
- تحقق تصميم الجدول: كل معلم تجريبي مرتبط بشعبة واحدة وبخطة 30 حصة، مع حد يومي صارم 6 حصص؛ العربية والرياضيات والعلوم والإنجليزية 5 حصص أسبوعيًا تسمح بتوزيعها مرة يوميًا.
- تحقق تصميم الاستراحات: 8 أوقات أساسية، فجوة 10 دقائق، وثلاث نوافذ ذكية غير متداخلة داخل محرك الجدول تستوعب 20 دقيقة لكل مجموعة، وكل شعبة تنتمي إلى مجموعة واحدة.
- تحقق سياسة الإيصالات: ترتيب تصاعدي، معرف مصدر للطباعة الإدارية، واستبعاد الحركة المحاسبية المكررة إذا غطاها إيصال عائلي canonical.
- تحقق جسر منصة التعلم في المصدر: هوية تعلم منفصلة لكل طالب/معلم مع دخول SSO من حساب OPAL ERP، وإتاحة عامة/صف/طالب، وربط الدورات بالمادة والشعبة الرسمية.

## ما لم يُشغل في بيئة البناء

Django غير مثبت في بيئة بناء الحزمة، لذلك **لم نزعم** تشغيل `manage.py check` أو migrations أو اختبارات قاعدة البيانات هنا. يجب تشغيل اختبارات R29 المستهدفة على PythonAnywhere بعد التركيب وقبل اعتماد التحديث أو الضغط على زر البيانات التجريبية.

الأمر المعتمد موجود في دليل التركيب ويشمل:

- عقد R29 للبيانات المترابطة.
- اختبار جسر ولي الأمر/الطالب والمعلم والإتاحة وSSO وتقييد الصف/الشعبة والبطاقة.
- عقد الإيصالات واختبارات سجل الإيصالات القائمة.

## معيار الاعتماد

لا تضغط زر إدخال البيانات التجريبية حتى تنجح على PythonAnywhere:

```text
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py migrate
اختبارات R29 → OK
```

بعد ذلك يمكن تشغيل زر البيانات التجريبية؛ الخدمة نفسها ذرية وتنفذ تحققًا نهائيًا للجدول/النصاب/الاستراحات قبل اعتماد العملية.
