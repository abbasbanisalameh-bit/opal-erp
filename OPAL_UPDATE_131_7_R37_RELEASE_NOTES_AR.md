# R37 — تطبيق نظام أوبال الأصلي على Android

- إنشاء تطبيق Flutter مستقل باسم **نظام أوبال** يمكن تثبيته بجانب تطبيق **منصة أوبال التعليمية**.
- اعتماد شعار مدرسة أوبال الدولية الدائري الرسمي الذي حدده المستخدم كشعار وأيقونة تطبيق النظام.
- تسجيل الدخول بنفس اسم المستخدم وكلمة المرور الموجودين في OPAL ERP.
- جلسات تطبيق مستقلة تبدأ بـ `oer_`، والرمز الخام لا يُحفظ في قاعدة البيانات؛ يُخزن Hash فقط.
- تخزين جلسة الهاتف في `flutter_secure_storage`.
- تحديد الوحدات تلقائيًا حسب دور المستخدم وصلاحياته.
- دعم لوحة التحكم، الطلاب، أولياء الأمور، المعلمين، الجدول، الحضور، الرسوم والدفعات، الامتحانات والعلامات، الوثائق والإعلانات.
- دعم البحث في الوحدات المناسبة.
- عزل بيانات المدرسة/الفرع والمعلم حيث يتوفر سياق رسمي في الحساب.
- إضافة API مستقل تحت `/mobile/api/v1/`.
- إضافة Workflow مستقل لبناء APK/AAB على GitHub Actions.
- لا تعديل على `students.Student` ولا إنشاء نموذج طالب بديل.
- عمليات البيانات في R37 قراءة فقط؛ أوامر الكتابة تؤجل حتى اعتماد التطبيق الأول ميدانيًا.
