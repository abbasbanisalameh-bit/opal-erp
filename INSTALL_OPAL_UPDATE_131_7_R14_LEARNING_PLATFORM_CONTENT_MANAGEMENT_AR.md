# تركيب OPAL Update 131.7 R14

يحوّل هذا التحديث لوحة مدير منصة أوبال التعليمية من شاشة متابعة فقط إلى **إدارة تشغيلية أساسية للمحتوى والحسابات**.

## ما يضيفه التحديث

- إدارة حسابات مستخدمي المنصة وعرض المتعلمين والمدرّسين.
- إنشاء حساب مدرّس مستقل بكلمة مرور أولية، دون إنشاء مستخدم OPAL ERP أو طالب مدرسي.
- تفعيل وإيقاف حسابات المتعلمين والمدرّسين عبر POST مدقق.
- إنشاء المواد التعليمية وتعديلها وتعطيلها.
- إنشاء الدورات كمسودات، وتحديد المادة والمدرّس والمستوى واللون والملخص.
- إنشاء الدروس وتعديلها وترتيبها ونشرها.
- نشر الدورة فقط بعد وجود درس منشور واحد على الأقل، ومع مدرّس ومادة فعالين.
- إعادة الدورة إلى مسودة أو أرشفتها دون حذف البيانات.

## حدود الفصل المحفوظة

- مدير المنصة يدخل من حساب OPAL ERP عبر `/learning/manage/`.
- المدرّس والمتعلم يدخلان من `/learning/login/`.
- التسجيل العام `/learning/register/` ينشئ متعلمًا فقط.
- لا تُنشأ حسابات OPAL ERP أو سجلات `students.Student` لمستخدمي المنصة.
- لا توجد نماذج موازية جديدة ولا هجرة قاعدة بيانات جديدة في R14.

## التركيب من مركز التحديثات

1. ارفع حزمة R14 من مركز التحديثات وثبّتها فوق R13.
2. بعد نجاح التثبيت اضغط Reload لتطبيق الويب من PythonAnywhere.
3. افتح منصة أوبال التعليمية من القائمة الجانبية بحساب المدير.
4. من شريط إدارة المنصة نفّذ التسلسل: **إضافة مدرّس → إضافة مادة → إنشاء دورة → إضافة درس → نشر الدورة**.

## أوامر التحقق على PythonAnywhere

```bash
cd /home/Opalschool2016/opal_school
source /home/Opalschool2016/.virtualenvs/opal/bin/activate
python tools/validate_opal_update_131_source.py .
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py migrate --noinput
python manage.py test learning_platform core.test_update131_7_r14_learning_platform_content_management_contract
python manage.py collectstatic --clear --noinput
```

> إن كان مسار البيئة الافتراضية مختلفًا، فعّل البيئة التي يعمل بها تطبيق OPAL على PythonAnywhere.

## اختبار القبول اليدوي

1. افتح `/learning/manage/` بحساب مدير OPAL ERP.
2. أنشئ حساب مدرّس، ثم سجّل دخوله من `/learning/login/` للتأكد من استقلال الحساب.
3. أنشئ مادة تعليمية.
4. أنشئ دورة؛ يجب أن تُحفظ كمسودة.
5. حاول نشرها قبل إضافة درس؛ يجب أن يرفض النظام.
6. أضف درسًا منشورًا ثم انشر الدورة.
7. افتح `/learning/courses/` وتأكد من ظهور الدورة المنشورة فقط.
8. أوقف حساب المدرّس وتأكد من منعه من الدخول.
