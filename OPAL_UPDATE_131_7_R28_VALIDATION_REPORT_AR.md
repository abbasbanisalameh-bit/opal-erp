# تقرير تحقق OPAL Update 131.7 R28

## الهوية

- الإصدار: `131.7`
- اسم الإصدار: `OPAL Update 131.7 R28 - Production Data Reset and Launch Preparation`
- خط الأساس: `OPAL Update 131.7 R27 - FileResponse Test Connection Isolation Fix`
- نوع الحزمة: `code_only`
- مراجعة الحزمة: `31`
- الهجرة الجديدة: `core.0014_productiondataresetrun`

## النطاق المنفذ

- معاينة مفصلة للبيانات التشغيلية قبل الحذف.
- رمز معاينة موقّع صالح لمدة 30 دقيقة، ومنع التنفيذ عند تغير البيانات بعد المعاينة.
- تأكيد صريح ومعاملة ذرية وتقرير نهائي دائم.
- حذف بيانات المدرسة التشغيلية وبيانات منصة التعلم والحسابات غير حساب المدير.
- إبقاء المدرسة والفروع والأدوار والصلاحيات وإعدادات التسجيل والوثائق والجدول وOpenEMIS ومساعد المنصة ومركز التطوير.
- إبقاء جلسة المدير الحالية وحذف الجلسات الأخرى.
- إزالة زر التصفير المباشر من واجهة إعدادات النظام ومنع استدعائه القديم من الخلفية.
- عدم حذف ملفات `media` وعدم إنشاء نسخة قاعدة بيانات تلقائية.

## التحقق الساكن في بيئة البناء

- تحليل تركيب جميع ملفات Python: ناجح.
- عدد ملفات Python المحللة: `641`.
- بوابة مصدر OPAL Update 131.7: `355/355` ناجحة، دون فشل أو تحذير.
- التحقق من جميع أسماء النماذج المستهدفة للحذف: `87/87` موجودة، دون تكرار.
- فحص المسارات والقوالب وهوية الإصدار: ناجح ضمن بوابة المصدر.
- فحص ZIP: ناجح، دون قاعدة بيانات أو وسائط أو بيئة افتراضية أو أسرار أو أرشيفات متداخلة.
- عدد ملفات الحزمة: `1335` ملفًا.

## اختبارات التشغيل المضافة

- المعاينة لا تحذف أي سجل وتصدر رمزًا موقّعًا.
- التنفيذ يحذف الطلاب وأولياء الأمور وبيانات المنصة والحسابات الأخرى.
- التنفيذ يبقي المدير والمدرسة والفروع والأدوار والصلاحيات والإعدادات ومركز التطوير.
- رفض المعاينة القديمة عند تغير البيانات.
- منع التنفيذ دون العبارة الدقيقة وخانة الإقرار.
- فتح تقرير نهائي بعد نجاح التنفيذ.

## قيد بيئة البناء

لم تتوفر Django واعتماديات المشروع في بيئة البناء، لذلك لم تُشغّل اختبارات Django Runtime محليًا. يجب تشغيلها على PythonAnywhere قبل تنفيذ حذف البيانات.

## التحقق المطلوب على PythonAnywhere

```bash
cd /home/Opalschool2016/opal_school
source venv/bin/activate

python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py migrate
python manage.py test \
  core.test_update131_7_r28_production_data_reset \
  core.test_update131_7_r28_production_launch_preparation_contract \
  --settings=config.settings_test_low_memory \
  --verbosity 1 \
  --noinput
python manage.py collectstatic --clear --noinput
```

لا يُنفذ حذف تلقائي عند تركيب التحديث. التنفيذ النهائي يحتاج فتح صفحة المعاينة وكتابة عبارة التأكيد.
