# تقرير تحقق OPAL Update 131.7 R2 — سلامة بيانات لوحة المدير

## نتائج التحقق في بيئة البناء

- بوابة فحص مصدر Update 131: **148 من 148 ناجحة**، دون أخطاء أو تحذيرات.
- تحليل تركيب Python: **574 ملفًا**، دون أخطاء.
- توازن قالب لوحة المدير: ناجح (`if/endif` و`for/endfor` و`block/endblock`).
- تدقيق غياب المخططات الصفرية المصطنعة `[0]`: ناجح.
- تدقيق نطاق المدرسة والعام في مصادر الطلاب والمعلمين والشعب والحضور والرسوم والعلامات: ناجح.
- تدقيق معادلات الرضا وعدم تكرار المشاركة: ناجح.
- تدقيق وجود أمر قاعدة البيانات `audit_dashboard_truth` وتغطيته للمؤشرات والقوائم والمخططات الحية: ناجح.
- فحص ZIP بعد إنشائه واستخراجه منطقيًا: ناجح.
- عدد ملفات الحزمة: **1089 ملفًا**.
- لا تحتوي الحزمة قاعدة بيانات أو `media` أو `.env` أو Git أو بيئة افتراضية أو `staticfiles` المجمعة.

## ما يغطيه أمر تدقيق الإنتاج

الأمر `python manage.py audit_dashboard_truth` يقارن القيم التي يبنيها سياق اللوحة مع المصادر المباشرة التالية:

- الطلاب النشطون والإجمالي والشعب والمعلمون.
- حضور اليوم وغياب ومغادرة الثلاثين يومًا وقائمة الغائبين.
- صافي الرسوم والمدفوع والمتبقي والتحصيل والمتأخر والإيرادات الشهرية.
- العدد والقائمة المؤهلة لمن لم يسددوا خلال 30 يومًا.
- العلامات الرسمية والمتوسط ونسبة الاجتياز وأوائل الصفوف.
- جميع متوسطات ونسب وتوزيعات ومصادر رضا المستخدمين.
- متوسط وترتيب ومخطط تقييم المعلمين للشهر الحالي.
- الشكاوى والتعاميم والإعلانات النشطة.
- غياب المعلمين، المشغولين، المتفرغين، ومصفوفة أحداث الصفوف والشعب.
- آخر عشرة قيود طلاب نشطة في العام الحالي.

## حدود التحقق هنا

بيئة البناء الحالية لا تحتوي Django ولا قاعدة بيانات PythonAnywhere الفعلية؛ لذلك لم تتم مقارنة أرقام قاعدة التشغيل من هذه البيئة. لا يُقدَّم ادعاء بخلاف ذلك. التحقق النهائي من الأرقام الحقيقية يتم داخل بيئة المشروع الفعلية بواسطة أمر التدقيق المرفق، وهو أمر قراءة فقط ويفشل عند وجود أي اختلاف.

## شرط الاعتماد النهائي على PythonAnywhere

```bash
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test \
  core.test_update131_7_r2_dashboard_truth_contract \
  core.test_update131_7_fixed_manager_dashboard_contract \
  dashboard.tests
python manage.py audit_dashboard_truth
```

الاعتماد النهائي يتطلب:

- `System check identified no issues`
- `No changes detected`
- `OK`
- `نجحت جميع تدقيقات لوحة المدير`
