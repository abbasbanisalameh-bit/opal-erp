# OPAL Update 107 — TPI وإصلاح ترحيل الحضور

## ما تم إنجازه

- إصلاح سبب ترحيل `attendance_v2` غير المطلوب: أصبح اسم فهرس سجل الحضور مطابقًا صراحةً للفهرس المنشأ في الترحيل السابق `0005`، لذلك لا يعود Django إلى اقتراح `RenameIndex` عند فحص الترحيلات أو رفع الكود إلى GitHub.
- إضافة حقول منفذ ووقت **الحفظ الفعلي** لسجل حضور الشعبة. فتح شاشة الحضور لا ينشئ سجلًا ولا يحتسب نشاطًا في TPI.
- إزالة بطاقة الإعلانات السفلية من لوحة ولي الأمر، مع الإبقاء على شريط الإعلانات المتحرك العلوي كما هو.
- إنشاء محرك TPI مركزي واحد داخل `teachers/tpi.py`، بأوزان ثابتة في الكود: تقييم ولي الأمر 18%، دوام المعلم 22%، متوسط العلامات 15%، النجاح والتحسن 15%، الواجبات 10%، إدخال العلامات في الموعد 10%، حضور الطلبة 5%، والنشاط المدرسي الحقيقي 5%.
- يستند TPI فقط إلى السجلات الرسمية: تقييمات أولياء الأمور، سجل دوام المعلم القائم، الامتحانات والعلامات، الواجبات، وسجل حضور الطلبة المحفوظ. لا يستخدم النقرات أو مدة فتح الصفحات.
- تم توسيع سجل `timetable.TeacherAbsence` نفسه ليكون سجل دوام المعلم اليومي؛ لم يُنشأ نموذج أو نظام دوام موازٍ. الأعذار والمهمات الرسمية المعتمدة مستثناة من التقييم، وكذلك أيام العطل الرسمية المعرّفة في التقويم الدراسي.
- تظهر للمعلم نتيجته وترتيبه وأسباب التقييم ومؤشرات التحسين واسم المعلم الأول، وتظهر العبارة: «أنت المعلم الأول لهذا الشهر» عند المركز الأول.
- تعرض لوحة إدارة المعلمين أفضل ثلاثة معلمين مع سبب موجز مبني على الأدلة.
- تحفظ لقطات TPI الشهرية المغلقة كما حُسبت؛ لا يعاد حسابها أو تغيير ترتيبها عند تعديل البيانات لاحقًا.
- تم تحديث استيراد OpenEMIS ليحترم سياسة حضور الطلبة المعتمدة (استثناءات فقط): الحضور العادي لا ينشئ صفًا موازيًا أو قديمًا.

## الترحيلات الجديدة

- `attendance_v2.0006_attendanceregister_submitted_at_and_more`
- `timetable.0006_teacherabsence_approved_by_and_more`
- `exams.0008_exam_marks_due_date`
- `teachers.0012_teacherperformancesnapshot`

ترحيل `timetable.0006` يحافظ على السجلات الموجودة: الغياب المبرر القديم يصبح عذرًا معتمدًا، وغير المبرر يبقى غيابًا مؤثرًا. لا يحتوي أي ترحيل على حذف بيانات أو استخدام `--fake`.

## التحقق المنفذ

- `python manage.py check`
- `python manage.py makemigrations --check --dry-run`
- `python manage.py migrate --noinput`
- كامل الاختبارات: 251 اختبارًا ناجحًا.

