# تقرير تحقق OPAL Update 131.0

**الحالة:** جاهز للتثبيت المرحلي المشروط باجتياز فحوص Django على PythonAnywhere  
**تاريخ التقرير:** 2026-07-29  
**خط الأساس المحلي:** commit `a7a251d`، وهو لقطة الكود المرفوعة لـ Update 130.0  
**خط الأساس المثبت في مركز النظام:** الفرع `opal-stable-baseline-20260714`، commit `6d1139c500c1`

## 1. فحوص المصدر المنفذة محليًا

| الفحص | النتيجة |
|---|---|
| تحليل جميع ملفات Python نحويًا | ناجح |
| بوابة المصدر `tools/validate_opal_update_131_source.py` | ناجح — 79/79 |
| اختبارات عقود Update 127 و129 و131 القياسية | ناجح — 19/19 |
| `git diff --check` | ناجح |
| `python -m compileall` لجميع ملفات Python | ناجح |
| عدم وجود قالب منشئ جدول مستقل | ناجح |
| عدم وجود قوالب Curriculum التشغيلية | ناجح |
| عدم وجود قوالب لوحة الامتحانات وقائمة العلامات الموازية | ناجح |
| عدم وجود روابط داخلية للمسارات القديمة | ناجح |
| بقاء `students.Student` النموذج الرسمي الوحيد | ناجح |

## 2. فحص قاعدة SQLite التجريبية قبل الهجرة

شُغّل:

```bash
python tools/opal_update_131_preflight.py db.sqlite3 --json
```

النتيجة: `ok: true`، دون أخطاء أو تحذيرات.

| المؤشر | القيمة |
|---|---:|
| حجم قاعدة البيانات | 9,965,568 بايت |
| المواد قبل الدمج | 72 |
| صفوف المادة السنوية المتوقعة | 72 |
| صفوف Curriculum التي ستحذف | 72 |
| تكليفات المعلمين | 144 |
| حصص الجدول | 336 |
| الامتحانات | 1,152 |
| النتائج الفصلية | 0 |
| النتائج السنوية | 0 |
| الشعب | 24 |
| لقطات بنية الفصل | 0 |

لم يكتشف الفحص:

- مادة مستخدمة بلا خطة أو قيمة حصص صالحة.
- تعارضًا بين عدد حصص Curriculum والتكليف.
- علاقة مادة بعام أو صف مختلف.
- تعارضًا ناتجًا عن توحيد اسم الشعبة.

## 3. فحص ألوان هوية المواد

حُسبت الهويات الموحدة من بيانات القاعدة التجريبية، وكانت ست هويات بألوان مختلفة بصريًا:

- اللغة العربية: `#DC2626`
- الرياضيات: `#2563EB`
- العلوم: `#16A34A`
- اللغة الإنجليزية: `#9333EA`
- التربية الإسلامية: `#EA580C`
- الحاسوب: `#0891B2`

نجح شرط المسافة البصرية الدنيا بين جميع الألوان. كما صُححت الهجرة لتعيد استخدام اللون نفسه عند تكرار هوية المادة عبر الصفوف بدل محاولة تخصيص لون جديد لها.

## 4. هوية الإصدار وسلامة المدخل الموحد

- `OPAL_VERSION.txt` يثبت الإصدار `131.0`.
- `OPAL_RELEASE_NAME.txt` يحمل الاسم الكامل للإصدار.
- `OPAL_UPDATE_MANIFEST.json` يثبت أن الحزمة code-only ومبنية على خط الأساس المعتمد.
- مركز التحديثات يفضل هوية الإصدار المحمولة مع الكود على مؤشر نشر خاص قديم، مع إبقاء وقت ومصدر النشر لأغراض التدقيق.
- بوابة الشؤون الأكاديمية تعرض بطاقة واحدة فقط للمواد والخطة، وبطاقة واحدة للجدول والمنشئ الذكي، وبطاقة واحدة للعلامات والتحليل.
- فُحصت مراجع مفاتيح دليل العمليات ولم يبق مفتاح `curriculum` أو `smart-timetable` أو `exam-analysis` تشغيلي موازٍ.

## 5. اختبارات العقود المنفذة

```bash
python -m unittest \
  core.test_update127_smart_timetable_contract \
  core.test_update129_horizontal_timetable_contract \
  core.test_update131_safe_consolidation_contract -v
```

النتيجة: **20 اختبارًا ناجحًا**.

تشمل العقود:

- دمج المنشئ الذكي في شاشة الجدول.
- بصمة المصادر والاعتماد الذري.
- حذف القوالب الموازية.
- وسم المسارات القديمة للحذف في Update 132.
- المصدر السنوي الواحد للمادة والخطة.
- سياسة دوام المعلم المبنية على الاستثناءات.
- الحساب المعياري للعلامات وحفظ جميع المتعادلين.
- وقت الحصة في الرأس، البحث عند 30 صفًا، والطباعة.
- الصيغة الموحدة لاسم الصف والشعبة.
- محرك TPI الرسمي `TPI-131`.

## 6. تحقق معماري

- لا نموذج Django جديد للطالب أو الجدول أو الحضور أو الألوان.
- `academics.Subject` هو المصدر التشغيلي للمادة والخطة.
- `TeacherAbsence` هو سجل استثناء الدوام الرسمي.
- `ClassCoverage` هو سجل الإشغال.
- `SchoolDayEvent` هو مصدر الاستراحات.
- `timetable.live_services` هو مصدر الحالة الحية.
- `teachers.tpi` و`TeacherPerformanceSnapshot` هما مرجع أداء المعلم.
- `/exams/` هو المدخل الإداري الموحد للعلامات والتحليل.

## 7. فحص الحزمة النهائية المعزولة

بُنيت الحزمة كملف ZIP جذري يحتوي `manage.py` مباشرة، ثم فُحصت بأداة:

```bash
python tools/verify_opal_update_131_archive.py OPAL_Update_131_0_SAFE_CONSOLIDATION_20260729.zip --json
```

النتيجة: ناجحة، وتحتوي الحزمة **1,003 ملفًا** دون قاعدة بيانات أو وسائط أو أسرار أو بيئة افتراضية أو بيانات Git أو ملفات static مجمعة أو أرشيفات متداخلة.

بعد ذلك فُكت الحزمة في مجلد تحقق مستقل، وأعيد تشغيل بوابة المصدر `79/79`، وتحليل Python، واختبارات العقود `20/20` على الملفات المستخرجة نفسها؛ وجميعها نجحت.


## تصحيح بوابة الهجرات بعد تجربة PythonAnywhere

كشفت بوابة مركز التحديثات الفعلية أن النموذج النهائي لـ `academics.Subject` كان سيولد هجرة إضافية لتعديل `canonical_key` و`grade`. أضيفت الهجرة الصريحة:

- `academics.0018_alter_subject_canonical_key_alter_subject_grade`

كما أضيف عقد ثابت داخل أداة التحقق واختبار العقود لمنع حذف هذه الهجرة أو عودة الانحراف بين `models.py` وحالة الهجرات. لا تعتمد الحزمة المصححة على تجاوز `makemigrations --check`؛ يجب أن يبقى الفحص الإلزامي ناجحًا في PythonAnywhere.

## 8. فحوص لم تنفذ في البيئة المحلية

لم تكن مكتبة Django مثبتة في بيئة العمل المحلية المرفقة، لذلك **لم تُنفذ محليًا** الأوامر التالية:

- `python manage.py check`
- `python manage.py makemigrations --check --dry-run`
- `python manage.py migrate --plan`
- `python manage.py migrate`
- مجموعة اختبارات Django الكاملة.
- `python manage.py collectstatic --noinput`

هذه ليست نتائج ناجحة ضمن هذا التقرير، بل بوابات إلزامية يجب تشغيلها في بيئة PythonAnywhere الافتراضية قبل إعادة تحميل الموقع. تعليمات التنفيذ والرجوع موجودة في `INSTALL_OPAL_UPDATE_131_SAFE_CONSOLIDATION_AR.md`.

## 9. قرار الاعتماد

الحزمة صالحة للانتقال إلى **مرحلة التثبيت المرحلي على نسخة أمان في PythonAnywhere**. لا تعتبر منشورة إنتاجيًا، ولا يجوز إعادة تحميل تطبيق Web، إلا بعد نجاح فحوص Django والهجرات والاختبارات و`collectstatic` في البيئة الفعلية.
