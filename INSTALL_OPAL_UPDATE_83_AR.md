# تركيب تحديث OPAL 83

## قبل البدء

1. خذ نسخة احتياطية حديثة من قاعدة البيانات و`media` من الخادم.
2. ارفع ملف `OPAL_UPDATE_83_INSTALL_20260723.zip` فقط إلى شاشة **إدارة النظام ← تحديثات النظام**.
3. راجع اسم الإصدار وملاحظاته، ثم نفّذ التحديث من الشاشة. تنشئ الشاشة لقطة أمان تلقائية، وتشغّل `migrate` و`collectstatic` و`check`.
4. بعد النجاح أعد تحميل خدمة الويب من لوحة الاستضافة إذا كانت الاستضافة تتطلب ذلك.

لا تفك الحزمة يدويًا فوق المشروع، ولا تستبدل `db.sqlite3` أو `media` أو `.env` أو البيئة الافتراضية.

## فحص سريع بعد التركيب

1. افتح مركز الدورة السنوية من الشؤون الأكاديمية وتحقق من العام والفصل المعروضين.
2. افتح مركز دورات الامتحان وتحقق من عدم ظهور تكرار في التكليفات.
3. افتح المركز المالي/الرواتب وتأكد من صلاحيات المدير فقط.
4. راجع حساب ولي أمر لديه التزام مالي للتأكد من أن سياسة الوصول ظاهرة بالتنبيه الافتراضي.

## التراجع

استخدم `OPAL_ROLLBACK_TO_82_20260723.zip` من شاشة **تحديثات النظام** عند الحاجة. حزمة التراجع تعيد الكود إلى نسخة ما قبل التحديث، لكنها لا تلغي تلقائيًا ترحيلات قاعدة البيانات الإضافية؛ لذلك استعد نسخة قاعدة البيانات التي أخذتها قبل التركيب إذا كان المطلوب تراجعًا كاملاً للبيانات والبنية.
