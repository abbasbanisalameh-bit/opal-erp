# تقرير تحقق Hotfix R3 لـ OPAL Update 131.0

التاريخ: 2026-07-29

## النطاق

يعالج هذا التصحيح انحدارين في شاشة **إدارة المعلمين ← عرض الجميع**:

1. إعادة احتساب TPI لجميع معلمي المدرسة عند كل فتح، وهو سبب حمل مرتفع وتأخير واضح.
2. اختفاء إجراء دخول المدير الأعلى إلى حساب المعلم المرتبط والنشط.

## الفحوص المنفذة في بيئة البناء

- تحليل نحوي لجميع ملفات Python: ناجح.
- بوابة المصدر القياسية لـ Update 131: **89/89 ناجحة**.
- تحقق ثابت من عدم استدعاء `management_tpi_context` داخل `teacher_list`: ناجح.
- تحقق ثابت من قراءة `TeacherPerformanceSnapshot` للشهر الحالي: ناجح.
- تحقق ثابت من وجود شرط المدير الأعلى ومسار `accounts:impersonate_user` وزر «الدخول بحسابه»: ناجح.
- لا توجد ملفات هجرة جديدة ولا تغيير في نماذج قاعدة البيانات.

## حدود التحقق المحلي

لم تكن Django مثبتة في بيئة البناء المحلية، لذلك لا يدعي هذا التقرير تنفيذ فحوص Django التشغيلية محليًا. يجب أن ينفذ مركز التحديثات في PythonAnywhere بواباته المعتادة، ثم يُتحقق من:

```bash
python manage.py check
python manage.py makemigrations --check --dry-run
```

## اختبار القبول بعد التثبيت

- تفتح قائمة المعلمين بسرعة طبيعية.
- يظهر زر «الدخول بحسابه» للمدير الأعلى فقط عند وجود حساب معلم نشط.
- ينجح الدخول بحساب المعلم والعودة إلى حساب المدير.
