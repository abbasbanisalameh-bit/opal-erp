# OPAL Update 131.5 R2 — مصمم لوحة المدير المبسّط

## الجديد

- أداة مركزية واحدة بدل قوائم تحكم داخل كل عنصر.
- تحديد عنصر واحد أو عدة عناصر أو جميع عناصر اللوحة.
- تطبيق العرض والارتفاع والشكل والحواف على العناصر المحددة دفعة واحدة.
- إظهار وإخفاء مجموعة عناصر بعملية واحدة.
- سحب عنصر واحد أو مجموعة محددة وتغيير ترتيبها.
- أزرار تقديم وتأخير للعناصر المحددة.
- ثلاثة تخطيطات جاهزة: مضغوط، متوازن، موسع.
- حفظ التفضيلات في `accounts.UserProfile.dashboard_layout` لاستعادتها من أي جهاز.
- إعادة آثار مخطط التخصيص القديم إلى التخطيط المتوازن تلقائيًا.

## الحماية

- لا يقبل الخادم إلا 29 معرف عنصر معتمدًا.
- العرض والارتفاع والشكل والحواف مقيدة بقوائم سماح ثابتة.
- لا يقبل CSS أو HTML حرًا ولا أسماء أصناف من المستخدم.
- لا يستخدم `localStorage` أو `sessionStorage` مصدرًا للحقيقة.
- حجم طلب الحفظ محدود، والترتيب يعاد تطبيعه على الخادم.

## قاعدة البيانات

لا يوجد نموذج أو جدول جديد. توجد هجرة واحدة تضيف حقل JSON إلى ملف المستخدم الحالي. الهجرة متوافقة مع حالة وجود العمود مسبقًا بعد استعادة كود جزئية.
