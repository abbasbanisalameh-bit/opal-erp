# تقرير تحقق OPAL Update 131.4 — مصفوفة الأحداث الحية والتوقيت الرسمي

## خط الأساس

- المصدر: الحزمة الكاملة `OPAL_Update_131_3_SUBJECT_PALETTE_HOTFIX_R1_20260730.zip`.
- التحديث الناتج: Code-only كامل، دون قاعدة بيانات أو وسائط أو أسرار أو بيئة افتراضية.

## النتائج المنفذة في بيئة البناء

- فحص المصدر القياسي: **126 من 126 ناجحة**.
- تحليل تركيب ملفات Python: **562 ملفًا دون SyntaxError**.
- اختبارات العقود القابلة للتشغيل دون Django: **29 من 29 ناجحة**.
- فحص JavaScript بواسطة `node --check`: ناجح.
- التحقق من هوية الإصدار وManifest ورمز كسر الكاش: ناجح.
- التحقق من وجود بوابة Django الجديدة `opal.E134`: ناجح.
- التحقق من عدم إنشاء نموذج أو صفحة أو مسار موازٍ: ناجح.

## العقود المضافة

- بنية أفقية من ثلاثة صفوف للصف والشعبة والحدث.
- `colspan` مطابق لعدد الشعب الفعلية.
- حالة مستقلة لكل شعبة.
- عمود عناوين ثابت وسحب أفقي على الهاتف.
- وقت رسمي مشتق من وقت الخادم.
- تحديث تلقائي عند أقرب حد زمني.
- حالات مستقلة قبل الدوام وبين الأحداث واليوم بلا جدول وبعد الدوام.
- منع `TimeSlot` العام من إنشاء حالة دوام وهمية بلا حصص فعلية.

## الفحوص التي يجب إكمالها على PythonAnywhere

بيئة البناء الحالية لا تحتوي Django، لذلك لم يُنفذ فيها `manage.py check` أو اختبارات قاعدة البيانات. يجب أن ينفذ مركز التحديثات أو Bash على PythonAnywhere:

```bash
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test core.test_update131_4_live_events_matrix_contract timetable.tests_live_operations
python manage.py collectstatic --noinput
```

لا يعتمد التحديث كنقطة استقرار ولا يرفع إلى GitHub قبل نجاح هذه الأوامر وفحص العرض أثناء حصة واستراحة وقبل/بعد الدوام.
