# تقرير تحقق OPAL Update 131.7 R30

## سبب الإصدار
أظهر اختبار R29 الفعلي على PythonAnywhere نجاح 11 اختبارًا وفشل 3 حالات محددة:
- إعادة ولي الأمر إلى `/parent/` بدل لوحة منصة التعلم.
- إعادة المعلم إلى `/teachers/portal/` بدل لوحة منصة التعلم.
- ظهور كلمة «طباعة» في النص الوصفي لمكوّن إيصالات ولي الأمر رغم أن الواجهة مطلوبة للقراءة فقط.

## الإصلاح
- إضافة `/learning/` إلى المسارات المسموح بها في `ParentPortalAccessMiddleware`.
- إضافة `/learning/` إلى المسارات المسموح بها في `TeacherPortalAccessMiddleware`.
- لا يتجاوز هذا صلاحيات منصة التعلم؛ الجلسة التعليمية والدور داخل `LearningAccount` يبقيان الحاكمين للوصول.
- إزالة كلمة «طباعة» من الوصف الظاهر لولي الأمر، مع إبقاء زر الإدارة المشروط بـ`can_print_receipts`.

## تحقق بيئة البناء
- Static source gate: `355/355 PASS`.
- Python files parsed: `647`.
- اختبارات العقود المحلية القابلة للتشغيل دون Django: `9/9 PASS`.
- فحص compileall للملفات المعدلة: ناجح.
- فحص سلامة ZIP عبر `verify_opal_update_131_archive.py`: ناجح بلا أخطاء أو تحذيرات.
- لا توجد migration جديدة.

## التحقق المطلوب على PythonAnywhere
يجب إعادة مجموعة الاختبارات التي فشلت في R29 مع عقد R30 الجديد. النجاح النهائي هو ظهور `OK` لكل المجموعة قبل تشغيل زر البيانات التجريبية في قاعدة التشغيل.
