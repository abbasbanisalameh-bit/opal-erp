# تقرير تحقق OPAL Update 131.3 — بيانات السعة وتوقيت الأردن

## النطاق المعتمد

هذا التحديث يطور **زر إدخال البيانات الحالي فقط** ليبني بيانات قبول مترابطة لـ500 طالب، ويضبط الخطة الأسبوعية ونصاب المعلمين، ويجعل ساعة النظام مرجعية من الخادم وفق `Asia/Amman` افتراضيًا لإربد.

لا يضيف التحديث نموذج Django أو جدول قاعدة بيانات أو صفحة أو مسارًا تشغيليًا أو هجرة جديدة.

## تحقق السعة والخطة

تم تثبيت المعادلة التشغيلية الآتية:

- 25 شعبة: ثلاث شعب للصف الأول، وشعبتان لكل صف من الثاني حتى الثاني عشر.
- 500 طالب: 20 طالبًا في كل شعبة.
- 19 حصة أسبوعية لكل شعبة.
- إجمالي الخطة: `25 × 19 = 475` حصة أسبوعية.
- 19 معلمًا، نصاب كل معلم 25 حصة: `19 × 25 = 475`.
- توزيع نصاب كل معلم على خمسة أيام: خمس حصص يوميًا بالضبط.
- سياسة الفراغ: حد أدنى حصة فراغ يومية، مع بقاء الفترات غير المسندة متاحة للتكليف أو الإسناد اليدوي لاحقًا.

تم اختبار بدائل المواد:

- الصفوف 1–3: اجتماعيات 3 بدل التاريخ والجغرافيا والتربية الوطنية.
- الصفوف 4–9: علوم 4، مع التاريخ والجغرافيا والتربية الوطنية.
- الصفوف 10–12: الفيزياء والكيمياء والأحياء وعلوم الأرض، حصة لكل مادة، بدل العلوم.
- التربية الرياضية 3، المهنية 2، الفنية 1 لجميع الصفوف.

## تحقق الجدول

شُغلت محاكاة حتمية لتوزيع التكليفات وبناء الجدول بالمعطيات نفسها التي يستخدمها زر البيانات:

- 329 تكليفًا.
- 475 حصة مجدولة.
- صفر حصة غير مسندة.
- جميع المعلمين التسعة عشر: 25 حصة أسبوعية وخمس حصص في كل يوم من الأحد إلى الخميس.
- بقاء ثمانية أوقات حصص أساسية يحافظ على الاستراحات المتدرجة ويترك مساحة تشغيلية لإضافات المدير.

## تحقق التوقيت

- المنطقة الافتراضية هي `Asia/Amman`.
- إعداد Django يستمد المنطقة من `OPAL_TIME_ZONE` مع رجوع آمن إلى توقيت الأردن.
- وقت الشريط العلوي يبدأ من وقت الخادم المرسل مع الصفحة، ثم يستمر في المتصفح دون الاعتماد على ساعة الجهاز أو منطقته الزمنية.
- خدمات الحصة الجارية والاستراحة والأحداث تعتمد وقت Django الرسمي نفسه.

## نتائج الفحوص المحلية

- فاحص المصدر: **116 من 116 ناجحة**.
- ملفات Python التي جرى تحليلها نحويًا: **559 ملفًا** دون خطأ.
- اختبارات العقود القياسية: **31 من 31 ناجحة**.
- فحص SQLite للقاعدة التجريبية المرفوعة: `quick_check = ok`.
- محاكاة السعة والجدول: ناجحة بـ475 حصة وصفر تعارض غير محلول.
- فحص محتوى ZIP النهائي: ناجح، ويمنع قاعدة البيانات و`media` و`.env` و`.git` والبيئة الافتراضية والملفات المجمعة.

## قيد بيئة البناء

مكتبة Django غير متاحة في بيئة البناء المحلية، لذلك لم أدّع تشغيل فحوص Django التنفيذية هنا. ينفذ مركز التحديثات وPythonAnywhere الفحوص النهائية التالية داخل البيئة الفعلية:

```bash
cd /home/Opalschool2016/opal_school
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test core.test_update131_3_capacity_timezone_contract
python manage.py collectstatic --noinput
```

النتائج المطلوبة:

```text
System check identified no issues
No changes detected
```

## قرار الاعتماد

الحزمة صالحة للرفع إلى **مركز تحديثات النظام** بوصفها تحديث كود فقط، مع عدم رفعها إلى GitHub إلا بعد نجاح فحوص PythonAnywhere وتجربة زر إدخال البيانات ووقت الأحداث فعليًا.
