# ملاحظات إصدار تجربة ولي الأمر ومركز التحديث الذاتي

الإصدار: `OPAL_GUARDIAN_EXPERIENCE_SELF_RELOAD_V1_20260718`

## الفكرة المعتمدة

التحديث يوحّد تجربة ولي الأمر ومركز التحديث في مسار واحد محافظ على البيانات، وفق قاعدة المشروع: واجهة عربية موحدة، مصدر رسمي واحد، منطق مشترك في الخدمات، وواجهات عرض خفيفة.

## ما تم اعتماده

- زر **إعادة تحميل الموقع** داخل مركز التحديث، للمدير الأعلى فقط، مع تأكيد وسجل تدقيق وتحديث تلقائي للصفحة بعد عشر ثوانٍ.
- إعادة التحميل عبر API عند وجود رمز آمن في بيئة الخادم، أو عبر ملف WSGI الرسمي عند عدم وجوده.
- اعتماد تسمية **ملفات أولياء الأمور** في إدارة الطلاب، و**حساب ولي الأمر فلان** في الملف المالي.
- استبدال أسماء العرض المرتبطة بمفهوم الأسرة أو العائلة، بما فيها اسم العائلة الذي أصبح **الاسم الأخير**.
- اعتماد **رقم ملف ولي الأمر** بدل رمز الأسرة، دون تغيير اسم العمود الداخلي أو قيمه القديمة.
- سجل إيصالات أفقي للقراءة فقط في لوحة ولي الأمر وصفحة الرسوم والملف الشامل.
- دمج ثلاثة مصادر للإيصالات: دفعة التسجيل الأولى، إيصالات المحاسبة السابقة، وتسديد الرسوم الموحّد.
- بيان كل إيصال: التاريخ، الوقت، القيمة، نوع التسديد، الأبناء، حصة كل ابن، المستلم وصفته، وحالة الإيصال.
- منع تكرار إيصال التسجيل عند ظهوره في مصدر المحاسبة القديم.
- تصحيح مؤشر الملف الشامل ليحسب حالة **مغادر** بدل الحالة القديمة غير المعتمدة.

## حماية البيانات والتوافق

- لم يُحذف أي نموذج أو جدول أو حقل أو إيصال.
- بقي النموذجان الداخليان `Family` و`FamilyStudent` كما هما حتى لا تنكسر العلاقات والتكاملات والتقارير القديمة.
- بقيت أسماء المسارات الداخلية متوافقة؛ التغيير في أسماء العرض العربية فقط.
- الترحيلان `admissions.0007_guardian_terminology` و`parent_portal.0009_guardian_terminology` يغيران بيانات الوصف في حالة Django فقط، بلا تعديل للبيانات التشغيلية.
- سجل ولي الأمر لا يتيح فتح الإيصال أو طباعته، ولا يعرض إيصالات أبناء غير مرتبطين بالحساب.

## التحقق المنفذ

- `manage.py check`: ناجح.
- `makemigrations --check --dry-run`: لا تغييرات غير مرحّلة.
- تطبيق جميع الترحيلات على قاعدة اختبار: ناجح.
- `collectstatic --noinput`: ناجح.
- 126 اختبارًا: ناجحة بالكامل.
- فحص القوالب والمصطلحات التشغيلية: ناجح، ولا تظهر التسميات المستبعدة في ملفات Python أو HTML التشغيلية خارج سجل الترحيلات التاريخي.
