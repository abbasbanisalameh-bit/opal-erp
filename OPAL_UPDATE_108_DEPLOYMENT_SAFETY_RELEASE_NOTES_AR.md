# OPAL Update 108.0 — بوابة النشر والعزل

## الهدف

هذا تحديث حماية وتجهيز للنشر. لا يضيف صفحة مدرسية، ولا يغيّر بيانات الطلاب أو العلامات أو الحضور أو التقييمات.

## ما تم إصلاحه

1. **مركز التطوير محمي بالكامل:** كل مسارات `/development/`، ومنها بيانات مخطط Gantt، أصبحت متاحة لمدير النظام الأعلى فقط. المستخدم غير المسجل يدخل صفحة الدخول، وأي حساب آخر يحصل على منع صلاحية.
2. **استعادة التحديث أصبحت تحمي قاعدة البيانات أيضًا:** قبل تطبيق أي تحديث، ينشئ مركز التحديث نسخة كود آمنة ونقطة أمان مستقلة لقاعدة SQLite. إذا أخفق `check` أو فحص الترحيلات أو `migrate` أو `collectstatic`، يعيد الكود وقاعدة البيانات إلى حالتهما قبل التحديث تلقائيًا.
3. **لا استخدام لـ `--fake`:** الاستعادة تستخدم الترحيلات الفعلية فقط، ثم تستعيد نسخة SQLite المنطقية عند الفشل.
4. **فحص التحديث صار بالترتيب الملزم:** `check` ثم `makemigrations --check` ثم `migrate` ثم `collectstatic` ثم `check` أخيرًا.
5. **تجهيز GitHub بأمان:** أضيف `.gitignore` ومنع داخلي لرفع قواعد البيانات وملفات البيئة والنسخ والسجلات والملفات المولدة. كما أضيف `.env.example` فارغ من الأسرار لشرح إعداد الإنتاج.
6. **تحديث محرك التحديث نفسه:** يثبت الترحيل التقني `core.0011_bootstrap_update_engine_v3` نقطة الدخول الدائمة الجديدة، حتى لا تعيد طبقة حماية قديمة الكتابة فوق إصلاحات مركز التحديث لاحقًا. لا ينشئ هذا الترحيل جداول ولا يمس بيانات العمل.

## ما لم يتغير

- لم يحذف التحديث أي بيانات أو جداول أو ترحيلات.
- بقي `students.Student` نموذج الطالب الرسمي الوحيد.
- لم يُنشأ مسار بديل لإصدار الوثائق أو العلامات أو التحديثات.
- لم تتغير هوية OPAL أو ملفات الواجهة الأساسية.

## تنبيه مهم قبل التشغيل الحقيقي

هذا التحديث لا يضع مفتاح إنتاج أو حساب مدير نظام من تلقاء نفسه؛ لأن ذلك يجب أن يكون تحت سيطرة مالك النظام. بعد التركيب، يضبط مدير النظام `OPAL_SECRET_KEY` في بيئة الخادم، ويتأكد من وجود مدير نظام فعّال، ثم يشغّل:

```bash
python manage.py audit_production_readiness --strict
```

لا يبدأ إدخال البيانات الحقيقية قبل أن تصبح نتيجة الفحص بلا أخطاء، وبعد حفظ نسخة احتياطية خارجية مستقلة.
