# OPAL ERP — إغلاق النواة والجاهزية للتشغيل

**الإصدار:** `OPAL_FINAL_CORE_COMPLETION_AND_PRODUCTION_READINESS_20260717_V1`  
**النسخة الأساس:** `OPAL_LOGIN_PERFORMANCE_STABILITY_20260717_V1`  
**التاريخ:** 2026-07-17

## النتيجة

هذا تحديث إكمال للنواة الحالية، وليس وحدة جديدة. حافظ على المصادر الرسمية
`students.Student` و`teachers.Teacher` و`core.AcademicYear` و`academics.Enrollment`
و`exams.StudentMark` و`attendance_v2.Attendance` و`accounting.StudentInvoice`، ولم
ينشئ نموذجًا أو مسار بيانات موازيًا.

## دورة نهاية العام الدراسي

- أضيفت حالة إغلاق رسمية إلى `core.AcademicYear` مع وقت الإغلاق ومنفذه وملاحظاته.
- لا يسمح الإغلاق بوجود قيد طالب نشط أو امتحان غير مغلق وغير مقفل.
- أضيف فحص واضح قبل الإغلاق يعرض الموانع والتنبيهات والأعداد المتأثرة.
- عند الإغلاق تُقفل سجلات الحضور، وتُوقف الشعب والتكليفات والواجبات والجدول والخطط
  الدراسية وإعدادات رسوم الصفوف الخاصة بالعام.
- تبقى الفواتير والأرصدة والتحصيل المالي متاحة بعد الإغلاق دون تغيير مبالغها.
- تبقى النتائج المغلقة ظاهرة لولي الأمر وفي التقارير بدل اختفائها عند تحويل حالة
  الامتحان من «منشور» إلى «مغلق».
- لا يمكن تعديل السجلات الأكاديمية لعام مغلق من الواجهات التشغيلية.
- أضيف تفعيل صريح للعام المفتوح كعام حالي، مع تحديد الفصل الحالي تلقائيًا.
- أصبح إنشاء عام جديد متاحًا حتى عند وجود أعوام سابقة، ويُنشأ له فصلان فقط.

## الترفيع والتخريج

- أصبحت شاشة الحركة الجماعية تدعم الترفيع أو التخريج الجماعي.
- تتحقق الشاشة من المدرسة والعام والصف والشعبة ومنع الاستهداف إلى عام مغلق.
- لا يمكن تمرير قيود من عام أو صف آخر عبر تعديل الطلب يدويًا.
- يسجل كل إجراء في `StudentLifecycleEvent` وسجل التدقيق التشغيلي.

## الصلاحيات وسلامة الشاشات

- حُصرت إدارة الأعوام والهيكل والصفوف والشعب والمواد بالمستخدمين الإداريين.
- أصبحت مصفوفة الصلاحيات متاحة لمدير النظام `superuser` فقط.
- حُصرت إدارة الخطة الدراسية بالإدارة، وأصبح الحذف بطلب `POST` محمي بـCSRF.
- أُصلح جدول الخطة الدراسية ليعرض حقول النموذج الفعلية بدل أسماء غير موجودة.
- مُنع تعديل التكليفات والجدول والحضور والامتحانات والخطة المرتبطة بعام مغلق.

## الجاهزية والأمان

- أصبح `DEBUG=False` هو الوضع الافتراضي الآمن، ويمكن تفعيله محليًا فقط عبر
  `OPAL_DEBUG=True`.
- أصبحت النطاقات المسموحة ومصادر CSRF قابلة للضبط من متغيرات البيئة.
- أضيف دعم رأس HTTPS القادم من الوكيل العكسي، وإعدادات ملفات الجلسة وCSRF الآمنة.
- أضيفت صفحات عربية آمنة لأخطاء 400 و403 و404 و500 دون عرض تشخيص Django.
- توسع أمر `verify_core_readiness` ليغطي إعدادات الإنتاج والترحيلات والحساب الإداري
  والعام الحالي والفصلين والهيكل والرسوم والقيود والامتحانات والجدول والحضور والبيانات
  التجريبية.
- يدعم تقرير الجاهزية نصًا عربيًا أو JSON، ووضع فحص صارم للتحذيرات.

## الترحيل

أضيف ترحيل واحد غير هدّام:

`core/migrations/0006_academic_year_closure.py`

يضيف فقط حقول الإغلاق إلى `core.AcademicYear` ولا يحذف أو ينقل أي بيانات حالية.

## نتائج التحقق

- `python manage.py check`: ناجح.
- `python manage.py makemigrations --check --dry-run`: لا توجد تغييرات ناقصة.
- تطبيق جميع الترحيلات على قاعدة الاختبار: ناجح.
- الاختبارات السابقة والجديدة: **116 اختبارًا ناجحًا**.
- فحص الجاهزية على قاعدة الحزمة الخالية مع `--allow-empty`: ناجح دون أخطاء حرجة.
- لم يُنفذ ربط حي مع OpenEMIS؛ بقي أساس تكامل ينتظر API وبيانات المصادقة الرسمية.

## حدود الإصدار

لا يتضمن الإصدار HR أو الرواتب أو LMS أو الذكاء الاصطناعي أو تغييرات شكلية، ولا
يحتوي على قاعدة بيانات أو ملفات `media` أو `.env` أو `.git` أو بيئة افتراضية.
