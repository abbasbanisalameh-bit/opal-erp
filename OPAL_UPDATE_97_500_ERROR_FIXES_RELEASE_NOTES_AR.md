# OPAL ERP — Update 97

## إصلاحات أخطاء صفحات 500

هذا التحديث مخصص حصراً لمعالجة مواضع مؤكدة قد تنتج عنها أخطاء خادم 500، دون إضافة ميزات جديدة أو تغيير تصميم النظام.

### الإصلاحات

1. إزالة دالة قديمة غير مرتبطة بالمسار الرسمي للإيصالات كانت تحاول عرض قالب محذوف:
   - `accounting/receipt_list.html`
   - المسار الرسمي يبقى محولاً إلى أرشيف إيصالات تسديد الرسوم المعتمد.

2. تأمين عرض أخطاء التحقق في قرار الخصم:
   - استبدال الاعتماد المباشر على `ValidationError.message` بمعالجة آمنة لـ `messages` أو النص البديل.

3. تأمين عرض أخطاء التحقق في حركة الطالب:
   - منع خطأ 500 عندما يعيد Django خطأ تحقق متعدد الرسائل أو غير مبني بصيغة رسالة مفردة.

### الملفات المعدلة

- `accounting/views.py`
- `academics/lifecycle_views.py`

### قاعدة البيانات

- لا توجد Migration.
- لا يوجد حذف أو تعديل للبيانات.

### الاختبار بعد التركيب

```bash
python manage.py check
python manage.py migrate
python manage.py collectstatic --noinput
```

ثم اختبار:

- فتح رابط أرشيف الإيصالات من النظام والتأكد من انتقاله إلى الأرشيف الموحد.
- تجربة رفض/اعتماد طلب خصم في حالة تحقق غير مكتملة والتأكد من ظهور رسالة بدلاً من صفحة 500.
- تجربة حركة طالب ببيانات غير صالحة والتأكد من ظهور خطأ داخل النموذج بدلاً من صفحة 500.
