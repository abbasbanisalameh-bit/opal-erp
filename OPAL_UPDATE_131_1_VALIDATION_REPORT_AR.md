# تقرير تحقق OPAL Update 131.1 — بوابة استقرار ما بعد الدمج

التاريخ: 2026-07-29

## خط الأساس

- OPAL Update 131.0 — Safe Consolidation & Production Readiness.
- يتضمن إصلاح R2 لقائمة المعلمين وإصلاح R3 للأداء وزر دخول المدير إلى حساب المعلم.

## الفحوص المنفذة في بيئة البناء

- تحليل نحوي لجميع ملفات Python بواسطة `compileall`: ناجح.
- بوابة المصدر القياسية بعد إضافة عقود الاستقرار: **99/99 ناجحة**.
- اختبارات العقود الثابتة المختارة: **22/22 ناجحة**.
- فحص سلامة نسخة SQLite التجريبية المرفوعة باستخدام `PRAGMA quick_check`: **ok**.
- لا توجد ملفات هجرة جديدة.
- لا توجد تغييرات في نماذج Django أو مخطط قاعدة البيانات.
- لا تحتوي الحزمة النهائية على قاعدة بيانات أو `media` أو `.env` أو بيئة افتراضية أو `.git` أو ملفات static المجمعة.

## العقود الجديدة المحمية

1. معالج الإشعارات العام للقراءة فقط ولا يستدعي مزامنة الحضور.
2. الشريط العلوي للإدارة يستخدم حالة المدرسة المختصرة، لا حالة الإدارة التفصيلية.
3. قائمة المعلمين ولوحتها مقيدتان بالمدرسة النشطة.
4. صفحات GET تقرأ لقطات TPI فقط.
5. إعادة حساب TPI الكاملة لا تتم إلا بطلب POST صريح من المدير.
6. فحص Django الجديد `opal.E131` يمنع رجوع هذه المخالفات.

## حدود التحقق المحلي

لم تكن مكتبة Django متاحة داخل بيئة البناء، لذلك لم يُنفذ محليًا:

```bash
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test
```

لا يدعي هذا التقرير نجاح تلك الأوامر محليًا. يجب أن ينفذها مركز تحديثات OPAL والبيئة الافتراضية الفعلية على PythonAnywhere قبل اعتماد الإصدار ورفعه إلى GitHub.

## اختبار القبول الإلزامي على الخادم

- فحص Django بلا أخطاء.
- لا تغييرات هجرات غير مسجلة.
- تحسن الاستجابة العامة بين الصفحات.
- عمل قائمة المعلمين وزر دخول المدير بحساب المعلم.
- عمل بوابة المعلم دون إعادة حساب TPI تلقائيًا.
- نجاح زر **تحديث TPI الآن**.
- استمرار عمل لوحة الجدول والحضور وإشعاراتهما من بواباتهما الرسمية.
