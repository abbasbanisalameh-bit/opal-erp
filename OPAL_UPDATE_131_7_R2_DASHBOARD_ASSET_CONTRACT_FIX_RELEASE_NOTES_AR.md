# OPAL Update 131.7 R2 — إصلاح عقد أصل لوحة المدير

هذا بناء مصحح من Update 131.7 R2.

## السبب
تغير رمز كسر ذاكرة التخزين المؤقت في `templates/base/base.html` إلى `20260803-update1317-r2-dashboard-truth`، بينما بقي فحص `opal.E136` يبحث عن الرمز القديم `update1317-fixed-crystal-dashboard`. لذلك فشل التحديث رغم وجود ملف `static/css/opal_dashboard_executive.css` وتحميله فعليًا.

## الإصلاح
- أصبح الفحص يتحقق من مسار ملف CSS الحقيقي بدل رمز إصدار مؤقت.
- تم تحديث اختبار Django الموافق.
- أضيف فحص للحزمة يمنع تكرار التعارض.
- لا توجد هجرات أو تغييرات بيانات.
- متوافق مع Django 5.2.16 ضمن القيد `Django>=5.0,<6.0`.
