# OPAL ERP — تحديث ثبات الدخول وتسريع لوحة التحكم

**الإصدار:** `OPAL_LOGIN_PERFORMANCE_STABILITY_20260717_V1`  
**النسخة الأساس:** `OPAL_ROLE_PERMISSIONS_ACADEMIC_WORKFLOW_20260716_V1`  
**النسخة المصدر:** `OPAL_CURRENT_20260717_040653` — الالتزام `0bd99fbed20a`  
**التاريخ:** 2026-07-17

## 1. إصلاح خطأ CSRF عند الدخول

- بقيت حماية Django CSRF مفعّلة بالكامل؛ لم تُعطّل ولم يُستثنَ تسجيل الدخول منها.
- أصبحت صفحة الدخول ترسل ترويسات تمنع تخزينها وإعادة استخدامها من ذاكرة المتصفح.
- عند الرجوع إلى صفحة دخول قديمة أو تبدّل رمز الجلسة، يرفض النظام الطلب القديم ثم يفتح صفحة دخول جديدة آمنة تلقائيًا.
- لا يعيد النظام إرسال اسم المستخدم أو كلمة المرور ولا يضعهما في الرابط.
- أضيفت صفحة OPAL عربية واضحة لأخطاء انتهاء صلاحية النماذج بدل صفحة Django التقنية.
- حُفظ مسار `next` الصحيح بعد تجديد صفحة الدخول.
- أصبح الرجوع من سجل المتصفح على الهواتف يجدد صفحة الدخول حتى إن لم يعلن المتصفح استخدام bfcache بالطريقة المعتادة.

## 2. تسريع لوحة الإدارة التنفيذية

- استُبدلت الاستعلامات المتكررة للحضور باستعلامات تجميعية واحدة.
- توقف تحميل كل الفواتير وكل العلامات إلى ذاكرة الخادم لحساب المؤشرات.
- حُسبت أعداد الطلاب والفواتير والحضور والنجاح داخل قاعدة البيانات مباشرة.
- أصبح اتجاه الحضور لسبعة أيام يُقرأ باستعلام واحد بدل استعلامين لكل يوم.
- أعيد استخدام نتائج تجميع الفواتير داخل الصفحة بدل تكرار الاستعلامات.
- انخفضت استعلامات بيانات اللوحة من **36 إلى 15**.
- انخفض إجمالي استعلامات استجابة صفحة اللوحة من **46 إلى 23** في قياس متطابق على النسخة الأساس والتحديث.
- التحسن أكبر مع زيادة البيانات لأن النسخة الجديدة لا تنشئ قائمة في الذاكرة من كل فاتورة وكل علامة.

## 3. تحسين تحميل الرسوم البيانية

- أزيل تحميل Chart.js من جميع صفحات النظام.
- أضيفت النسخة المحلية المثبتة `Chart.js 4.5.1` إلى لوحة التحكم فقط.
- لا تعتمد رسوم لوحة التحكم بعد التحديث على استجابة CDN خارجي.
- بقي Bootstrap وهوية OPAL وبنية ملفات الواجهة الأساسية دون تغيير تصميمي.

## 4. التوافق

- الطالب الرسمي الوحيد بقي `students.Student`.
- لم تُنشأ نماذج طالب أو معلم أو أسرة أو علامة أو حضور أو فاتورة بديلة.
- لا توجد ترحيلات قاعدة بيانات جديدة في هذا التحديث.
- لم تُعدّل بيانات النظام أو قاعدة البيانات أو ملفات `media`.
- أُبقيت علامات التوافق الخاصة بالجداول حتى تنجح اختبارات مركز التحديثات القديمة والجديدة.

## 5. نتائج التحقق

- `python manage.py check`: ناجح.
- `python manage.py makemigrations --check --dry-run`: لا توجد تغييرات ناقصة.
- إنشاء قاعدة اختبار وتركيب جميع الترحيلات: ناجح.
- اختبارات CSRF الجديدة: ناجحة.
- اختبارات مؤشرات لوحة التحكم وقيد عدد الاستعلامات: ناجحة.
- الاختبارات الكاملة: **98 اختبارًا ناجحًا**.
- `collectstatic --noinput --clear`: **131 ملفًا**.

## 6. التحقق بعد التركيب

1. اضغط Reload من صفحة Web في PythonAnywhere.
2. افتح رابط النظام وسجل الدخول بحساب الإدارة.
3. جرّب الرجوع إلى صفحة الدخول القديمة ثم تسجيل الدخول؛ يجب أن تتجدد الصفحة بدل ظهور خطأ 403 التقني.
4. افتح لوحة التحكم وتأكد من ظهور البطاقات والجداول والرسوم البيانية.
5. اختبر الدخول من الهاتف نفسه الذي ظهرت عليه المشكلة.
