# تركيب تحديث تجربة ولي الأمر ومركز التحديث الذاتي

الإصدار: `OPAL_GUARDIAN_EXPERIENCE_SELF_RELOAD_V1_20260718`

## التركيب من مركز التحديث

1. افتح **إعدادات النظام ← مركز تحديثات النظام** بحساب المدير الأعلى.
2. أنشئ نسخة كود احتياطية من الوضع الحالي.
3. ارفع ملف `OPAL_GUARDIAN_EXPERIENCE_SELF_RELOAD_V1_20260718.zip`.
4. بعد نجاح التحقق اختر **استعادة** واكتب كلمة التأكيد `استعادة`.
5. ينتظر المدير حتى تنتهي الترحيلات وتجميع الملفات الثابتة والفحص التلقائي.
6. لهذا التحديث فقط: نفّذ Reload يدويًا من صفحة Web في PythonAnywhere مرة أخيرة، لأن مسار الزر الجديد لا يدخل ذاكرة الخادم إلا بعد إعادة التشغيل الأولى.

بعد هذه المرة يظهر زر **إعادة تحميل الموقع** داخل مركز التحديث، ويحدّث الصفحة تلقائيًا بعد إعادة تشغيل الموقع، فلا تحتاج إلى صفحة Web في التحديثات اللاحقة.

## إعداد إعادة التحميل

في نطاق PythonAnywhere القياسي يستنتج OPAL النطاق وملف WSGI تلقائيًا ولا يحتاج إلى رمز API. إذا لم يعمل الاستنتاج يمكن تعريف:

```text
OPAL_PYTHONANYWHERE_DOMAIN=opalschool2016.pythonanywhere.com
OPAL_PYTHONANYWHERE_WSGI_FILE=/var/www/opalschool2016_pythonanywhere_com_wsgi.py
```

يمكن اختياريًا استخدام واجهة PythonAnywhere بدل لمس ملف WSGI عبر متغيرات البيئة التالية، ولا يوضع الرمز داخل الكود أو ملف ZIP:

```text
OPAL_PYTHONANYWHERE_USERNAME=opalschool2016
OPAL_PYTHONANYWHERE_API_TOKEN=ضع_الرمز_في_بيئة_الخادم_فقط
```

## اختبار القبول السريع

- افتح مركز التحديث وتأكد من ظهور زر **إعادة تحميل الموقع** للمدير الأعلى فقط.
- افتح **إدارة الطلاب ← ملفات أولياء الأمور** وتأكد من المصطلحات الجديدة.
- ادخل بحساب ولي أمر يملك أكثر من ابن، وافتح لوحة الحساب أو الرسوم.
- تأكد من ظهور سجل أفقي من الأحدث إلى الأقدم، وفي كل بطاقة: التاريخ، الوقت، المبلغ، الأبناء، توزيع المبلغ، طريقة التسديد، والمستلم.
- تأكد من عدم وجود رابط لفتح الإيصال أو طباعته في هذا السجل.

## الرجوع

يمكن استعادة نسخة الأمان التي أنشأها المركز قبل التحديث. ترحيلا هذا الإصدار يغيران أسماء العرض فقط ولا يحذفان أعمدة أو سجلات.
