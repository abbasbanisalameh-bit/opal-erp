# OPAL Update 131.1 — بوابة استقرار ما بعد الدمج

التاريخ: 2026-07-29  
النوع: تحديث كود فقط، بلا هجرات وبلا تعديل مباشر للبيانات  
خط الأساس: OPAL Update 131.0 مع Hotfix R3

## الهدف

تثبيت النظام بعد الدمج الكبير في Update 131، ومعالجة مصادر البطء التي كانت تعمل أثناء فتح صفحات عادية، ومنع عودة خطأ قائمة المعلمين أو اختفاء إجراء دخول المدير إلى حساب المعلم.

## الإصلاحات المنفذة

### 1. منع الكتابة من معالج الإشعارات العام

أُلغي تشغيل `sync_attendance_registers` من `enterprise_ops.context_processors.enterprise_notifications`.

كان هذا المعالج يعمل مع كل صفحة مصادقة، وقد ينفذ مسحًا وتحديثًا لسجلات الحضور وإشعارات الإدارة أثناء فتح صفحة لا علاقة لها بالحضور. أصبحت معالجات السياق العامة للقراءة فقط، وبقيت مزامنة الحضور داخل بوابتي الحضور والتقرير الرسميتين.

### 2. تخفيف حالة الجدول في الشريط العلوي

لم يعد الشريط العلوي للإدارة يستدعي `management_live_status` الكامل على كل صفحة. أصبح يستخدم `school_live_status` المختصر فقط.

الحالة التفصيلية التي تحسب الحصص الجارية، تفرغ المعلمين، الاستثناءات، الشعب، وملخص الصفوف بقيت في صفحات الجدول ولوحة الإدارة التي تعرضها فعليًا.

### 3. جعل عرض TPI للقراءة فقط

- لوحة إدارة المعلمين تقرأ آخر لقطات `TeacherPerformanceSnapshot` المحسوبة بدل إعادة حساب المدرسة عند كل فتح.
- بوابة المعلم تقرأ لقطة المعلم الحالية دون إعادة حساب كل معلمي المدرسة عند تسجيل الدخول.
- أضيف زر **تحديث TPI الآن** داخل لوحة إدارة المعلمين نفسها. التحديث الكامل أصبح إجراء `POST` صريحًا ينفذه المدير عند الحاجة، دون صفحة أو مسار جديد.

### 4. تقييد إدارة المعلمين بالمدرسة النشطة

قوائم المعلمين، التكليفات الحالية، العدادات، ولقطات TPI أصبحت مقيدة بالمدرسة المستخرجة من سياق الطلب. يمنع ذلك خلط بيانات المدارس ويقلل حجم الاستعلامات.

### 5. تقليل استعلام زائد في الحالة الحية

أزيل تنفيذ `exists()` ثم إعادة تنفيذ الاستعلام نفسه لجلب حصص الوقت؛ تُحمّل النتائج مرة واحدة ويُستخدم البديل فقط عند خلوها.

### 6. بوابة منع رجوع الأخطاء

أضيف فحص نظام جديد بالمعرف `opal.E131` يحمي القواعد الآتية:

- لا مزامنة حضور أو كتابة داخل معالج الإشعارات العام.
- لا استخدام لحالة الإدارة التفصيلية داخل الشريط العلوي العام.
- عرض TPI في صفحات GET للقراءة فقط.
- إعادة الحساب تتم بإجراء POST صريح.
- إدارة المعلمين مقيدة بالمدرسة النشطة.

## ما لم يتغير

- لا نماذج Django جديدة.
- لا جداول أو حقول أو هجرات جديدة.
- لا تعديل على سياسة الحضور أو TPI أو أوزانه.
- لا حذف للمسارات القديمة؛ تبقى تحويلات موسومة للحذف في Update 132.0.
- زر **الدخول بحسابه** بقي للمدير الأعلى فقط، ولحساب المعلم المرتبط والنشط.
- لم تُضف ميزات تجميلية أو وظائف خارج نطاق الاستقرار.

## اختبار القبول بعد التثبيت

1. فتح صفحات النظام المختلفة وملاحظة تحسن زمن الاستجابة العام.
2. فتح **إدارة المعلمين** و**عرض الجميع** دون خطأ 500.
3. ظهور زر **الدخول بحسابه** للمدير الأعلى عند تحقق شروطه.
4. فتح بوابة المعلم دون إعادة حساب TPI تلقائيًا.
5. الضغط على **تحديث TPI الآن** مرة واحدة والتأكد من ظهور رسالة النجاح.
6. التحقق من الحضور ولوحة الجدول والحالة الحية.
