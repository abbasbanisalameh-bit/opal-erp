# تقرير تحقق OPAL Update 131.5 R5 — قفل الهوية اللونية

## سبب الإصلاح
أثبتت المراجعة أن R4 صحح عقود التحقق، لكنه لم يجعل عودة الألوان الأصلية حتمية في جميع الحالات؛ إذ يمكن لتخطيط محفوظ باختيار «مسطح/إطار/زجاجي» أو لملف CSS مخزن مؤقتًا في الهاتف أن يبقي بطاقات التواصل دون هويتها اللونية الأصلية.

## المعالجة الجذرية
- قفل مظهر بطاقات `action-complaints` و`action-broadcasts` و`action-announcements` على `default` في التحقق الخادمي.
- تطبيق القفل نفسه في JavaScript عند القراءة والتعديل والحفظ.
- إضافة علامة `data-opal-color-lock="true"` صريحة إلى البطاقات الثلاث.
- تثبيت ألوان الأيقونة والسهم والبيانات الوصفية والدائرة الخلفية بقواعد CSS عالية الأولوية.
- تغيير مفاتيح cache-busting للـCSS والـJavaScript حتى يجلب الهاتف الملفات الجديدة بدل نسخة مخزنة.
- الإبقاء على العرض والارتفاع والترتيب والحواف والإخفاء قابلة للتخصيص.

## نتائج التحقق المحلي
- فحص المصدر: **147 من 147 ناجحة**.
- تحليل تركيب Python: **566 ملفًا دون أخطاء**.
- اختبار عقد لوحة المدير: **11 من 11 ناجحًا**.
- فحص JavaScript بواسطة `node --check`: **ناجح**.
- لا توجد هجرات أو تغييرات بيانات جديدة.

## حدود بيئة البناء
Django غير مثبت في بيئة البناء، لذلك يجب أن ينفذ مركز التحديثات `python manage.py check` و`makemigrations --check` داخل PythonAnywhere قبل اعتماد الحزمة.
