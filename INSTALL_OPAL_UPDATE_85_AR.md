# تركيب تحديث OPAL 85

1. خذ نسخة احتياطية من قاعدة البيانات و`media`.
2. من **إدارة النظام ← تحديثات النظام** ارفع ملف `OPAL_UPDATE_85_ROLE_UX_NOTIFICATIONS_20260723.zip` ثم نفّذ التحديث.
3. نفّذ ترحيلات قاعدة البيانات عند طلب النظام أو من الطرفية:

   ```bash
   python manage.py migrate
   ```

4. أعد تحميل خدمة الويب إذا تطلبت الاستضافة ذلك، ثم نفّذ تحديثًا قويًا للمتصفح (`Ctrl+F5`) لمسح نسخة CSS وJavaScript المخزنة.

## تحقق سريع بعد التركيب

- ادخل بحساب معلم: لا تظهر التقارير ولا سجل العمليات، وتظهر موادي وطلابي وامتحاناتي وجدولي.
- اطلب سلفة من حساب معلم: يصل للمدير إشعار يفتح الطلب نفسه، ثم تبقى الرسالة في مركز الإشعارات.
- ادخل بحساب ولي أمر: اختر ابنًا قبل عرض علاماته أو واجباته، ثم جرّب تقييم أحد معلميه.
- من حساب المدير: افتح لوحة التحكم وتحقق من أعلى ثلاثة معلمين والرسم البياني بعد وجود تقييمات.
- جرّب رفع صورة تتجاوز 1 م.ب للطالب أو المعلم وتأكد من ظهور رسالة الحد.

الحزمة لا تتضمن قاعدة البيانات أو `media` أو `.env` أو البيئة الافتراضية.
