# OPAL Update 131.7 R23 — ملاحظات الإصدار

## المشكلة المعالجة

كان manifest بعد R22 يحمل اسم R22، بينما بقي `OPAL_RELEASE_NAME.txt` في الخادم باسم R21. العقود المركزية كشفت هذا الانحراف بصورة صحيحة، ولذلك فشلت ثلاثة اختبارات هوية فقط.

## الإصلاح

- هجرة Core رقم `0012` تكتب هوية R23 المتسقة في الملفات الثلاثة.
- مركز التحديثات ينسخ ملفات الهوية صراحة ويتحقق من اتساقها قبل إكمال الاستعادة.
- عقد R23 يثبت الإصلاح ويمنع رجوع الانحراف.

## أثر البيانات

لا توجد تعديلات على نماذج أو جداول بيانات الأعمال. الهجرة تنفذ مزامنة ملفات إصدار فقط.
