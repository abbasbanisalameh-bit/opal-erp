# OPAL ERP SAFE CONSOLIDATION — 2026-07-13

## قرارات محفوظة

- بقي `students.Student` النموذج الرسمي الوحيد.
- بقيت جميع وحدات المدرسة الفعلية، ولم تُعد كتابة أي وحدة قائمة.
- بقيت الموافقات المرتبطة بالخصومات داخل `enterprise_ops` مع إخفاء إنشاء
  الطلبات العامة غير المرتبطة بوظيفة حقيقية.
- بقي OpenEMIS كأساس اختياري مخفي افتراضيًا.
- بقي مركز التطوير كاملًا، لكنه فُصل عن إعدادات وURL موقع المدرسة.

## توحيد آمن

- توحيد الأسرة على `Family/FamilyStudent` وخدمة ذرية واحدة.
- ترحيل علاقات `ParentProfile` قبل حذف النموذج.
- ترحيل إشعارات البوابة إلى إشعارات `enterprise_ops`.
- حفظ الرسائل القديمة في سجل العمليات قبل حذف نموذجها غير المستخدم.
- توحيد الرقم الوطني لولي الأمر على `guardian_national_id`.
- جعل الفواتير والدفعات المصدر المالي الحي الوحيد.
- ترحيل `Student.fees_total` و`Student.fees_paid` إلى فواتير ودفعات قبل حذف
  حقلي قاعدة البيانات، مع إبقاء خصائص قراءة مشتقة للتوافق.

## تنظيف مثبت بالاعتماديات

- إزالة تطبيقات `attendance` و`finance` و`school` الفارغة؛ الوحدة الفعلية
  للحضور هي `attendance_v2` والوحدة الفعلية للرسوم هي `accounting`.
- إزالة قوالب academics القديمة وصفحة coming soon غير المستخدمة.
- إزالة CSS وJavaScript والصورة الثابتة التي لا يشير إليها أي قالب.
- دمج سكربتي تعبئة مركز التطوير في الأمر الآمن والمتكرر
  `seed_development_center`.
- نقل وثائق V4–V10 التاريخية إلى `docs/archive/`.

## شروط الاعتماد

لا يعتمد الإصدار إلا بعد نجاح `check`، وفحص الهجرات، والترحيل على قاعدة
جديدة، واختبارات موقع المدرسة ومركز التطوير، و`collectstatic`، وفحص الجاهزية.
