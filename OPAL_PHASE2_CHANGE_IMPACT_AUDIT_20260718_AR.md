# تقرير فحص الارتباطات ومخاطر الإزالة — OPAL ERP

التاريخ: 2026-07-18  
القاعدة: `OPAL_TEMPLATE_CONSOLIDATION_PHASE_1_20260718`

## القرار التنفيذي

الإزالة الكاملة من قاعدة البيانات غير آمنة للوحدات المالية الأساسية. تم اعتماد
«إزالة تشغيلية آمنة»: حذف الروابط والقوالب المتكررة، وإيقاف مسارات الإنشاء
القديمة وتحويلها إلى المسار الموحد، مع إبقاء المحرك والسجلات التاريخية.

| العنصر المطلوب إزالته | أهم الارتباطات | نتيجة الحذف الكامل | القرار المطبق |
|---|---|---|---|
| فئات الرسوم `FeeCategory` | كل `StudentInvoice` مرتبط بها بعلاقة `PROTECT`، والتسجيل ينشئ/يستخدم فئة تلقائيًا | يتعذر حذف الفئات المرتبطة، ويتوقف إنشاء رسوم التسجيل | إزالة الشاشة والقالب فقط؛ إبقاء Model والبيانات |
| رسوم الطلاب/إضافة رسوم `StudentInvoice` | التسجيل، التسديد، رصيد الطالب والأسرة، Student 360، بوابة ولي الأمر، اللوحات، التقارير، إغلاق العام، فحص سلامة البيانات | تعطيل كامل للأرصدة والتسديد والتقارير المالية | إيقاف الإدارة اليدوية القديمة؛ إبقاء الإنشاء الآلي والمحرك |
| دفعة الطالب القديمة `StudentPayment` | المتبقي وحالة الفاتورة، إيصال التسجيل، توزيع دفعة الأسرة، التقارير | لا يمكن معرفة المدفوع والمتبقي، ويتوقف التسجيل ذو الدفعة الأولى | إيقاف الشاشة القديمة وتوجيه المستخدم إلى «تسديد رسوم» |
| الإيصالات القديمة `Receipt` | علاقة واحد لواحد مع الدفعة، و`StudentRegistration` يحتفظ بمرجع إليها | فقد مرجع إيصالات التسجيل وتعطل الطباعة التاريخية | إزالة قائمة الإيصالات القديمة؛ إبقاء Model والطباعة التاريخية وأرشيف التسديد المعتمد |
| الأقساط `Installment` | مرتبطة بالفاتورة وتظهر في كشف الطالب التاريخي | حذفها لا يوقف الرصيد الأساسي، لكنه يفقد خطة الاستحقاق التاريخية | إيقاف الشاشة والإنشاء، إبقاء السجلات الداخلية |
| طلبات الخصم `DiscountRequest` | الفاتورة ومحرك موافقات `enterprise_ops.WorkflowRequest` | فقد سجل الموافقات والقرارات القديمة؛ لا تتعطل الدفعات الأساسية | إيقاف الإنشاء والقائمة وإخفاء طلباتها من مركز الموافقات؛ إبقاء التاريخ |
| الخطة الدراسية `Curriculum` | العام والصف والمادة، إغلاق العام وفحص الجاهزية | حذف التطبيق مباشرة يكسر استيرادات إغلاق العام والاختبارات ويفقد عدد الحصص الأسبوعية التاريخي | إيقاف الشاشة والمسارات وحذف القوالب؛ إبقاء الجدول التاريخي |

## ما يبقى معتمدًا في الرسوم

- رسوم الصف تضبط من `academics:academic_structure` عبر `admissions.GradeFee`.
- التسجيل ينشئ الالتزام المالي والدفعة الأولى تلقائيًا.
- التحصيل يتم من `admissions:fee_payment_create` باسم «تسديد رسوم».
- الأرشيف والطباعة من `admissions:fee_payment_archive`.
- `StudentInvoice` و`StudentPayment` هما مصدر الحقيقة الداخلي للأرصدة، ولا
  يظهران كمسارين يدويين متنافسين.

## ما لن يتعطل بعد إيقاف الخطة الدراسية

الصفوف والشعب والمواد وتكليفات المعلمين والامتحانات والعلامات والجدول الدراسي
تبقى عاملة؛ لأنها تعتمد على `academics.Grade` و`academics.Section` و
`academics.Subject`، لا على نموذج الخطة الدراسية.

## القوالب المحذوفة في هذه المرحلة

1. `templates/accounting/fee_category_list.html`
2. `templates/accounting/invoice_list.html`
3. `templates/accounting/form.html`
4. `templates/accounting/installment_list.html`
5. `templates/accounting/discount_list.html`
6. `templates/accounting/receipt_list.html`
7. `templates/curriculum/curriculum_list.html`
8. `templates/curriculum/curriculum_form.html`
9. `templates/attendance_v2/take_attendance.html`

لا توجد في هذه المرحلة عملية حذف لسجلات الطلاب أو الأسر أو الرسوم أو الدفعات
أو الإيصالات أو الخطط التاريخية.
