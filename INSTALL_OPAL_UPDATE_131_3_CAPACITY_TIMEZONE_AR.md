# تثبيت OPAL Update 131.3 — بيانات السعة وتوقيت الأردن

## قبل التثبيت

1. يجب أن تكون النسخة الحالية Update 131.2 أو أحدث من سلسلة 131.
2. لا ترفع إلا ملف ZIP الخاص بالتحديث.
3. لا تستخدم زر إدخال البيانات إن كنت تريد الاحتفاظ ببيانات تشغيلية؛ الزر يصفر البيانات التشغيلية ثم يبني بيانات القبول الجديدة داخل معاملة واحدة.

## التثبيت من مركز التحديثات

1. افتح **مركز تحديثات النظام**.
2. اختر ملف:
   `OPAL_Update_131_3_CAPACITY_DATASET_JORDAN_TIME_20260729.zip`
3. اضغط **رفع التحديث والتحقق منه**.
4. انتظر نجاح الفحص والاستعادة الآمنة.
5. اضغط **إعادة تحميل الموقع** مرة واحدة.

## فحوص PythonAnywhere بعد التثبيت

```bash
cd /home/Opalschool2016/opal_school
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test core.test_update131_3_capacity_timezone_contract
python manage.py collectstatic --noinput
```

المطلوب:

```text
System check identified no issues
No changes detected
```

لا توجد هجرات جديدة في هذا التحديث.

## اختبار التوقيت

1. قارن ساعة الشريط العلوي بوقت الأردن الرسمي.
2. غيّر المنطقة الزمنية للهاتف مؤقتًا أو افتح النظام من جهاز مضبوط على منطقة أخرى؛ يجب أن تبقى ساعة OPAL على توقيت الأردن.
3. افتح لوحة المدير أثناء حصة أو استراحة وتأكد أن الحدث الجاري يطابق وقت الأردن.

المنطقة الافتراضية هي:

```text
Asia/Amman
```

لا يحتاج تشغيل إربد إلى أي إعداد إضافي. وعند نشر النظام مستقبلًا في منطقة أخرى، يضبط المسؤول متغير `OPAL_TIME_ZONE` في بيئة تطبيق الويب أو ملف WSGI ثم ينفذ Reload؛ لا يكفي أمر `export` مؤقت داخل Bash Console.

## اختبار زر إدخال البيانات

استخدم الزر فقط لأن البيانات الحالية تجريبية:

1. الإعدادات والسلامة ← إعدادات النظام.
2. اضغط **إدخال بيانات مترابطة واختبار آخر تحديث**.
3. انتظر رسالة النجاح؛ العملية أكبر من النسخة السابقة لأنها تنشئ أكثر من 52 ألف علامة مترابطة.
4. تحقق من النتائج:
   - 500 طالب.
   - 300 ولي أمر.
   - 19 معلمًا.
   - 25 شعبة.
   - 329 تكليفًا.
   - 475 حصة مجدولة.
   - نصاب كل معلم 25 حصة.
   - خمس حصص لكل معلم في كل يوم دوام.
   - حد أدنى فراغ يومي قابل للتعديل.

## اختبار المواد

- الصفوف 1–3: اجتماعيات، ولا يظهر تاريخ/جغرافيا/تربية وطنية.
- الصفوف 4–9: علوم 4 حصص، مع تاريخ وجغرافيا وتربية وطنية.
- الصفوف 10–12: فيزياء وكيمياء وأحياء وعلوم أرض بدل العلوم.
- الرياضة 3، المهني 2، الفن 1 في جميع الصفوف.

## قبل الرفع إلى GitHub

لا ترفع إلى GitHub قبل نجاح الفحوص واختبار زر البيانات والتوقيت. بعد النجاح احفظ النسخة الحالية وارفعها باعتبارها نقطة الاستقرار الجديدة.
