# OPAL Update 131.7 R30
## Learning SSO Gateway and Guardian Receipt Read-Only Fix

هذا إصدار إصلاحي مباشر لـR29 بعد الاختبار الفعلي على PythonAnywhere.

- يسمح لحساب ولي الأمر بعبور طبقة حصر البوابة إلى `/learning/` بعد اختيار الابن، بينما تبقى هوية التعلم مستقلة لكل ابن.
- يسمح لحساب المعلم بعبور طبقة حصر بوابة المعلم إلى `/learning/`، مع بقاء إنشاء المحتوى مقيدًا بالتكليفات الرسمية.
- يزيل عبارة «الطباعة للإدارة» من واجهة ولي الأمر حتى تكون الواجهة قراءة فقط بصورة صريحة، بينما تبقى وظيفة الطباعة متاحة للإدارة عبر شرط `can_print_receipts`.
- لا migration جديدة.
- لا حذف أو تعديل للبيانات التشغيلية.
- لا تغيير على `students.Student`.
