# ملاحظات إصدار OPAL Update 131.7 R6

- إصلاح استعلام مركز العمليات الذي كان يستخدم `OuterRef('subject_id')` على نموذج المادة.
- جعل حلّ الشعبة الموحّد يستخدم التطبيع نفسه الذي يفرضه نموذج `Section`، وبذلك يصبح استيراد OpenEMIS المتكرر idempotent.
- منع `KeyError` في تصدير لوحة الإدارة عند غياب أعلام توافر البيانات؛ تعرض القيم حينها كغير متاحة بدل صفر مصطنع.
- إغلاق `FileResponse` صراحة في اختبار تنزيل النسخ قبل تنظيف المجلد المؤقت.
- توحيد هوية الإصدار ومفاتيح cache الأساسية، وتقوية مدقق الحزمة ضد النشر الجزئي.

لم تُضف أي ميزة جديدة، ولم يُنشأ نموذج طالب أو نظام مالي موازٍ.
