# OPAL Update 109.0 — الاستقرار وتبسيط الواجهة

## هذا هو التحديث الذي يُركّب

هذه حزمة كاملة موحدة. إذا لم تكن قد ركبت Update 108، **ركّب Update 109 فقط**؛ فهو يتضمن حماية Update 108 أيضًا. لا تركّب الحزمتين فوق بعضهما.

## ما تم تحسينه

1. **شاشة رسوم الطلاب أصبحت أخف وأسرع:** كانت الشاشة تستدعي حساب المدفوع والمتبقي من قاعدة البيانات أكثر من مرة لكل رسم ظاهر. أصبحت تستعمل الحساب المركزي الواحد الذي تم تجهيزه مسبقًا، فيقل زمن الفتح كلما زاد عدد الطلاب والدفعات.
2. **القسط لا يغيّر بياناته بمجرد فتح القائمة:** كانت صفحة الأقساط قد تحفظ حالات في قاعدة البيانات أثناء العرض فقط. الآن تعرض الحالة الفعلية مباشرة، ولا تكتب في قاعدة البيانات إلا عندما يحصل إجراء مالي مقصود.
3. **مصدر مالي واحد على الشاشة:** صفحة رسوم الطلاب تعرض المدفوع والمتبقي والحالة من لقطة الحساب المالية المركزية نفسها، فلا تختلف البطاقة عن الجدول.
4. **مركز التحديث أبسط:** أزيل JavaScript قديم كان يعيد بناء أزرار تنزيل/حذف النسخ بعد تحميل الصفحة. الأزرار الرسمية الموجودة في القالب هي المسار الوحيد الآن، بلا إعادة بناء أو روابط صلبة مكررة.
5. **وصول تحديث الواجهة مضمون:** تم تغيير رقم إصدار ملف JavaScript في القالب، حتى لا يبقى المتصفح على نسخة قديمة مخزنة بعد تركيب التحديث.

## ما يتضمنه من Update 108

- مركز التطوير وبيانات Gantt محميان لمدير النظام الأعلى فقط.
- التحديثات التالية تنشئ نقطة أمان لقاعدة SQLite وتعيد الكود والقاعدة عند فشل الفحص أو الترحيل.
- مركز التحديث ينفذ: `check` ثم `makemigrations --check` ثم `migrate` ثم `collectstatic` ثم `check` أخيرًا.
- حماية Git من قواعد البيانات وملفات الأسرار والنسخ والملفات المولدة.

## سلامة البيانات

- لا حذف بيانات أو جداول أو وظائف.
- لا استخدام لـ `--fake`.
- لا نموذج طالب إضافي؛ المصدر الرسمي يبقى `students.Student`.
- لا يضيف التحديث حسابات عامة أو دفتر يومية أو دفتر أستاذ.

## الترحيلات

لا يوجد ترحيل جديد خاص بـ 109. إذا لم يكن Update 108 مركبًا، سيُطبَّق فقط الترحيل التقني `core.0011_bootstrap_update_engine_v3` لتثبيت محرك التحديث الآمن. لا ينشئ جداول ولا يمس بيانات المدرسة.
