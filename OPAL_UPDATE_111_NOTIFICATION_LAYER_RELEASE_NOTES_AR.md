# OPAL Update 111.0 — إصلاح طبقات الإشعارات العامة

## نقطة الأساس

هذه حزمة كاملة مبنية مباشرة فوق **OPAL Update 110.0 — إصلاح مركز إدخال وتصفير البيانات**، وتتضمن جميع ملفاته.

## سبب المشكلة

كان شريط التنقل الفرعي للوحدات يستخدم طبقة عرض `z-index: 1040`، بينما كان الشريط العلوي كله داخل طبقة أقل مقدارها `1030`. لذلك لم يكن رفع طبقة قائمة الإشعارات وحدها كافيًا؛ لأن القائمة بقيت محصورة داخل طبقة الشريط العلوي، وظهرت أزرار الوحدة فوقها في صفحات الخطة الدراسية وغيرها.

## الإصلاح المعماري

- رفع طبقة الشريط العلوي العامة إلى `1045` لتكون أعلى من جميع أشرطة الصفحة اللاصقة التي تنتهي عند `1040`.
- إبقاء طبقة الشريط العلوي أقل من طبقات Bootstrap الخاصة بخلفية النوافذ المنبثقة والنوافذ نفسها، وأقل من القائمة الجانبية المتنقلة؛ لذلك لا يسبب الإصلاح تداخلًا مع الحوارات أو قائمة الهاتف.
- تثبيت `overflow: visible` على جميع أسلاف قائمة الإشعارات داخل الشريط العلوي، لمنع قص القائمة في أي صفحة أو قياس شاشة.
- إنشاء سياق طبقات واضح باستخدام `isolation: isolate` بدل الاعتماد على ترتيب عناصر الصفحة مصادفةً.
- رفع طبقة قائمة الإشعارات داخل سياق الشريط العلوي عند فتحها.
- تحديث مفتاح Cache لملف CSS حتى يظهر الإصلاح مباشرة بعد `collectstatic` دون بقاء النسخة القديمة في المتصفح.

## نطاق الإصلاح

الإصلاح مركزي في القالب العام وملف الشريط العلوي المشترك؛ لذلك يطبق تلقائيًا على جميع صفحات المدير والمعلم وولي الأمر التي تستخدم `templates/base/base.html`، ولا توجد معالجة خاصة بصفحة الخطة الدراسية.

## الاختبارات

أضيف اختبار تعاقدي يتحقق من أن:

1. طبقة الشريط العلوي أعلى من طبقة شريط الوحدة اللاصق.
2. أسلاف قائمة الإشعارات لا تقصها.
3. القالب العام يستخدم مفتاح Cache الجديد.

## الترحيلات

لا توجد Migration جديدة ولا تغيير في البيانات أو النماذج.
