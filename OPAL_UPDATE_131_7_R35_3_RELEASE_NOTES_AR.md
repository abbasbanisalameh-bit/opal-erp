# OPAL Update 131.7 R35.3 — Flutter Manager Dashboard Syntax Hotfix

- إصلاح إغلاق `cards.map(...)` قبل `.toList()` في لوحة الإدارة داخل Flutter.
- معالجة الأخطاء الخمسة التي ظهرت في `flutter analyze` كسلسلة ناتجة عن خطأ نحوي واحد.
- إعادة تنسيق كتلة بطاقات إحصاءات الإدارة لتكون أوضح وأقل عرضة لأخطاء الأقواس.
- رفع نسخة تطبيق Android إلى `1.2.1+38`.
- لا توجد Migration جديدة ولا تغيير على قاعدة البيانات أو صلاحيات الإدارة أو SSO أو البصمة.
