# تركيب OPAL Update 131.7 R15

هذا تحديث إصلاحي محدود لخطأ 500 ظهر في عدة صفحات من منصة أوبال التعليمية بعد إنشاء دورة بمعرف عربي.

## سبب الخطأ

نماذج المادة والدورة والدرس تسمح بمعرفات Unicode العربية، لكن مسار عرض الدورة كان يستخدم محول Django من النوع `<slug:slug>` الذي يقبل في نمط المسار الأحرف اللاتينية والأرقام والشرطة والشرطة السفلية فقط. لذلك فشل توليد رابط دورة مثل:

`كورس-اللغة-الانجليزية-للصف-الثاني-ثانوي`

وأدى ذلك إلى `NoReverseMatch` في كل قالب يعرض رابط الدورة، ومنها الصفحة الرئيسية ودليل الدورات وقائمة دورات المدير ولوحة المدرّس والمتعلم.

## ما يصلحه R15

- تغيير مسار عرض الدورة إلى `<str:slug>` ليقبل المعرف العربي المخزن أصلًا.
- الحفاظ على المسار نفسه `/learning/courses/<slug>/` وعلى جميع الروابط الحالية.
- دعم الدورات القديمة ذات المعرف الإنجليزي والدورات الحالية ذات المعرف العربي.
- إضافة اختبار تراجع يفتح الصفحة الرئيسية ودليل الدورات وتفاصيل الدورة وقائمة المدير بدورة ذات معرف عربي.
- لا توجد هجرة قاعدة بيانات ولا تعديل على النماذج أو البيانات.

## التركيب من مركز التحديثات

1. ارفع حزمة R15 وثبّتها فوق R14.
2. بعد نجاح التثبيت اضغط **Reload** لتطبيق الويب من صفحة Web في PythonAnywhere.
3. افتح `/learning/` ثم `/learning/manage/courses/`.
4. افتح الدورة العربية المنشأة سابقًا وتأكد من ظهور تفاصيلها دون خطأ 500.

## أوامر التحقق على PythonAnywhere

```bash
cd /home/Opalschool2016/opal_school
source venv/bin/activate
python tools/validate_opal_update_131_source.py .
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test learning_platform core.test_update131_7_r15_learning_platform_arabic_slug_runtime_fix_contract
python manage.py collectstatic --clear --noinput
```

إذا كانت البيئة الافتراضية في مسار مختلف، فعّل البيئة المرتبطة بتطبيق الويب.
