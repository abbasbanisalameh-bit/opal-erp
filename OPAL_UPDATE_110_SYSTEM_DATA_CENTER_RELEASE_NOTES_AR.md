# OPAL Update 110.0 — إصلاح مركز إدخال وتصفير البيانات

## نقطة الأساس

هذه الحزمة الكاملة مبنية مباشرة فوق **OPAL Update 109.0 — الاستقرار وتبسيط الواجهة**، وتتضمن كل ملفاته؛ لذلك تُركَّب وحدها فوق النسخة الحالية ولا تحتاج إلى إعادة تركيب 109.

## سبب العطل

بعد إضافة سجل الحضور اليومي ومؤشر أداء المعلمين والتقييمات الشهرية، بقيت خدمة التصفير القديمة لا تحذف بعض السجلات التابعة قبل المعلم والهيكل الدراسي. أهمها `TeacherPerformanceSnapshot` الذي يحمي المعلم بعلاقة `PROTECT`. ولأن زر إدخال البيانات التجريبية يبدأ بتصفير البيانات الحالية داخل معاملة واحدة، كان العطل نفسه يوقف الزرين.

## ما تم إصلاحه

1. حذف لقطات TPI الشهرية قبل حذف المعلمين، منعًا لخطأ `ProtectedError`.
2. حذف `AttendanceRegister` والتقييمات الشهرية للخدمات ضمن التصفير الرسمي.
3. الإبقاء على حساب المدير الأعلى الذي نفّذ العملية فقط، مع الإبقاء على بنية الأدوار ومصفوفة الصلاحيات `RolePermissionRule`.
4. التحقق من حساب المدير الأعلى قبل بدء أي حذف، بدل بدء العملية ثم اكتشاف غياب الحساب في نهايتها.
5. استمرار التصفير والإدخال داخل `transaction.atomic`؛ عند أي خطأ تُلغى العملية كاملة ولا يبقى حذف أو إدخال جزئي.
6. إضافة معالجة أخطاء لزر إدخال البيانات التجريبية أيضًا، فلا تظهر صفحة 500، بل رسالة واضحة مع تسجيل السبب في سجل الخادم.
7. إظهار حالة «جارٍ التنفيذ» وتعطيل الزر بعد الإرسال لمنع النقر المكرر وإيضاح أن العملية الطويلة بدأت.

## توافق البيانات التجريبية مع النظام الحالي

- حضور الطلبة أصبح استثنائيًا فقط: تُنشأ سجلات للغائب والمغادر فقط، ولا تُنشأ حالات `present` أو `late` القديمة.
- يُنشأ سجل `AttendanceRegister` رسمي لكل شعبة ويوم مع منفذ ووقت الحفظ، حتى تظهر بيانات واقعية في مؤشر TPI.
- يُستخدم `timetable.TeacherAbsence` نفسه لدوام المعلم: منتظم، متأخر، مغادرة مبكرة، غياب، عذر معتمد، ومهمة رسمية.
- تُنشأ تقييمات أولياء الأمور للمعلمين وتقييمات الخدمات الإلكترونية وفق البنية الموحدة الحالية.
- ترتبط الامتحانات بالتكليف التدريسي الرسمي `TeacherAssignment` والشعبة والمعلم، مع مواعيد إرسال ونشر؛ لذلك تصبح العلامات التجريبية قابلة للاستخدام في TPI.
- لا يوجد نموذج طالب جديد؛ الطالب الرسمي يبقى `students.Student`.

## أدوات سطر الأوامر

أُصلح أمرا `seed_demo_school` و`reset_demo_school` ليحددا المدير الأعلى الذي سيبقى. إذا كان هناك مدير أعلى واحد يُختار تلقائيًا، وإلا يجب تمرير `--user USERNAME`.

## الترحيلات

لا توجد Migration جديدة في Update 110؛ التعديل في منطق الخدمة والاختبارات والواجهة فقط.

## فحوص الحزمة

- نجح فحص بناء جميع ملفات Python عبر `compileall`.
- أضيفت اختبارات تغطي لقطات TPI، سجل الحضور اليومي، تقييمات الخدمات، بقاء الصلاحيات، ورسائل فشل الزرين دون خطأ 500.
- تعذر تشغيل فحوص Django داخل بيئة التجهيز لعدم توفر حزمة Django فيها؛ مركز تحديث OPAL سيشغّل تلقائيًا `check` و`makemigrations --check` و`migrate` و`collectstatic` قبل اعتماد التحديث، مع الاستعادة الآلية عند الفشل.
