# تركيب OPAL Update 108.0 — بوابة النشر والعزل

## قبل التركيب

1. سجّل الدخول بحساب **مدير النظام الأعلى**.
2. من **إعدادات النظام ← مركز تحديثات النظام**، أنشئ نسخة من الكود الحالي.
3. لا تحذف قاعدة البيانات ولا تستخدم `--fake`.

## التركيب

1. ارفع ملف ZIP الخاص بهذا التحديث في **مركز تحديثات النظام**.
2. بعد ظهور الحزمة في القائمة، اختر **استعادة** واكتب `استعادة`.
3. انتظر حتى ينهي المركز: `check` ثم فحص الترحيلات ثم `migrate` ثم `collectstatic` ثم الفحص الأخير.
4. إذا نجح التحديث، اضغط **Reload** من صفحة Web في PythonAnywhere.

إذا أخفق أي فحص، يعيد المركز الكود وقاعدة SQLite تلقائيًا إلى ما قبل التحديث، ويعرض سبب الإخفاق. لا تعاود المحاولة قبل قراءة الرسالة.

## بعد التركيب

1. تأكد أن رابط `/development/gantt/data/` لا يعرض أي بيانات قبل تسجيل دخول مدير النظام الأعلى.
2. تأكد أن حساب موظف عادي لا يستطيع فتح مركز التطوير.
3. في إعدادات خادم الإنتاج، انسخ القيم المطلوبة من `.env.example` واضبط `OPAL_SECRET_KEY` بقيمة سرية قوية.
4. شغّل من الطرفية:

```bash
python manage.py audit_production_readiness --strict
```

حل أي خطأ يظهر قبل إدخال البيانات التشغيلية الحقيقية.
