# تقرير تحقق OPAL Update 131.7 R26

## الهوية

- الإصدار: `131.7`
- اسم الإصدار: `OPAL Update 131.7 R26 - Core Snapshot Test Isolation Fix`
- خط الأساس: `OPAL Update 131.7 R25 - Core Test Stability and Legacy Contract Fix`
- نوع الحزمة: `code_only`
- رقم مراجعة الحزمة: `29`
- هجرات قاعدة البيانات الجديدة: لا توجد.

## سبب الإصلاح

أظهر اختبار تطبيق `core` على PythonAnywhere أن 292 اختبارًا اكتملت، ثم وقع خطأ واحد:

```text
django.db.utils.ProgrammingError: Cannot operate on a closed database
```

كان السبب أن اختبارات نقطة أمان SQLite تستدعي `connections.close_all()` ضمن قاعدة اختبار Django المشتركة. أُبقي سلوك الإنتاج كما هو، وعُزل هذا الاستدعاء داخل الاختبارات باستخدام mock حتى لا تتأثر الاختبارات اللاحقة.

## النطاق المنفذ

- عزل `connections.close_all()` في اختبارات إنشاء واستعادة واحتفاظ نقاط أمان SQLite.
- إضافة `config/settings_test_low_memory.py` رسميًا للمشروع بدل إنشائه يدويًا على الخادم.
- إضافة عقد مصدر R26 يمنع رجوع المشكلة.
- تحديث بوابة المصدر لتتطلب مراجعة الحزمة 29 وتتحقق من ملفات R26.
- لا تغييرات على بيانات المستخدمين أو الطلبة أو الدورات أو الاشتراكات أو الصلاحيات.

## التحقق الساكن

- بوابة مصدر OPAL: `338/338` ناجحة.
- تحليل تركيب ملفات Python: `635` ملفًا دون أخطاء.
- تطابق `OPAL_RELEASE_NAME.txt` مع `OPAL_UPDATE_MANIFEST.json`: ناجح.
- مراجعة الحزمة: `29`.
- وجود ثلاث نقاط عزل لاتصال الاختبار في `DatabaseSafetySnapshotTests`: ناجح.
- وجود إعداد الذاكرة المنخفضة وعقد R26: ناجح.

## ما لم يُنفذ في بيئة البناء

لم تتوفر Django واعتماديات المشروع في بيئة البناء، لذلك لم تُشغّل اختبارات Django Runtime محليًا. يجب تنفيذ الاختبار المستهدف ثم اختبار `core` كاملًا على PythonAnywhere بعد التركيب.

## قرار التحقق

الحزمة اجتازت التحقق الساكن، لكنها لا تُعتمد نهائيًا قبل نجاح أوامر Django على PythonAnywhere وظهور `OK` في اختبار الإصلاح ثم اختبار تطبيق `core` كاملًا.
