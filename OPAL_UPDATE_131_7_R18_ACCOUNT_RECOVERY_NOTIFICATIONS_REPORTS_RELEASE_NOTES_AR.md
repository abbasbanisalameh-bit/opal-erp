# ملاحظات إصدار OPAL Update 131.7 R18

## النتيجة الوظيفية

أصبحت منصة أوبال التعليمية تدعم دورة الحساب والتواصل والتقارير التشغيلية:

`المستخدم يطلب استعادة الحساب → يصل رابط مؤقت إذا كان البريد مهيأ → تُغيّر كلمة المرور وتُبطل الجلسات القديمة → يستقبل المتعلم والمدرس إشعارات مرتبطة بالتعلم → يتابع مدير OPAL مؤشرات التشغيل ويصدر كشف التسجيلات CSV`.

## ما أضيف

- استعادة ذاتية مستقلة لحسابات المتعلم والمدرس، دون استخدام حسابات OPAL ERP.
- رمز استعادة عشوائي لا يُخزن بصورته الأصلية؛ يُخزن HMAC فقط، بمدة صلاحية قابلة للضبط.
- استجابة عامة لا تكشف إن كان البريد موجودًا في قاعدة المنصة.
- حد طلبات زمني لتقليل إساءة استخدام صفحة الاستعادة.
- إبطال جميع جلسات المنصة السابقة بعد تغيير كلمة المرور عبر `auth_version`.
- مركز إداري لطلبات الاستعادة وحالة إرسال البريد.
- إنشاء كلمة مرور مؤقتة عشوائية من مدير OPAL، تُعرض مرة واحدة ولا تُخزن كنص صريح.
- مركز إشعارات مستقل للمتعلم والمدرس مع عداد غير المقروء.
- إشعارات التسجيل في دورة، نتيجة الاختبار، إرسال الواجب للمدرس، تصحيح الواجب، إصدار الشهادة، تفعيل الاشتراك، واقتراب انتهاء الاشتراك أو انتهائه.
- منع تكرار الإشعارات الزمنية باستخدام مفتاح idempotency فريد.
- مركز تقارير تشغيلية بمرشحات التاريخ والمادة والدورة.
- مؤشرات الحسابات الجديدة، التسجيلات، الإكمال، متوسط التقدم، الاشتراكات الفعالة، الواجبات المنتظرة، ونسبة النجاح.
- تفصيل أداء الدورات وتنبيه الاشتراكات التي تنتهي خلال سبعة أيام.
- تصدير تسجيلات المتعلمين بصيغة CSV عربية UTF-8 مع حماية من CSV Formula Injection.

## مصادر الحقيقة

- الحساب التعليمي: `LearningAccount`.
- طلب الاستعادة: `LearningPasswordResetRequest`.
- الإشعار: `LearningNotification`.
- سجل التدقيق: `LearningAuditEvent`.
- التقرير: قراءة مباشرة من الحسابات والتسجيلات والاشتراكات والتسليمات الرسمية؛ لا يخزن أرقامًا موازية.

## إعداد البريد

إرسال البريد معطل افتراضيًا حتى لا يوهم النظام بنجاح التسليم دون SMTP حقيقي. عند عدم تهيئة البريد، يُسجل الطلب بحالة «تعذر الإرسال» ويستطيع المدير إنشاء كلمة مرور مؤقتة من مركز الحسابات.

متغيرات البيئة المدعومة:

- `OPAL_LEARNING_EMAIL_ENABLED=True`
- `OPAL_LEARNING_EMAIL_HOST`
- `OPAL_LEARNING_EMAIL_PORT`، الافتراضي 587
- `OPAL_LEARNING_EMAIL_USER`
- `OPAL_LEARNING_EMAIL_PASSWORD`
- `OPAL_LEARNING_EMAIL_USE_TLS=True`
- `OPAL_LEARNING_EMAIL_USE_SSL=False`
- `OPAL_LEARNING_DEFAULT_FROM_EMAIL`
- `OPAL_LEARNING_PASSWORD_RESET_MINUTES`، الافتراضي 60

## الضوابط

- مدير المنصة يبقى داخل حساب OPAL ERP ولا يملك حساب منصة موازٍ.
- المتعلم والمدرس يستخدمان صفحة دخول واستعادة مستقلة.
- روابط الإشعارات لا تعيد التوجيه خارج نطاق `/learning/`.
- جميع تغييرات حالة الإشعارات وكلمات المرور تستخدم POST وCSRF.
- إيقاف الحساب يبطل إصدار جلسته حتى لا تعود جلسة قديمة بعد إعادة التفعيل.
