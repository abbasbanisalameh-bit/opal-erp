# تثبيت OPAL Update 131.5 R4 — الإصلاح الجذري لعقود لوحة المدير

## السبب
فشل R3 لأن فحصين قديمين داخل Django كانا يتناقضان مع قرار واجهة أحدث، وفحص ثالث كان يعتمد على صياغة حرفية للكود بدل سلوكه. مركز التحديثات تصرف بصورة صحيحة وأعاد R2.

## ما يصلحه R4
- يجعل إخفاء اختصار الطلبات الإدارية الداخلية قرارًا واحدًا معتمدًا في القالب وعقد المدخل الموحد.
- يبقي المسار والخدمة والبيانات دون حذف.
- يستبدل فحص الأمان النصي الهش بفحص سلوكي يمرر بيانات خبيثة إلى دالة التطبيع ويتحقق من رفضها.
- يضيف فحوص بناء تمنع إصدار حزمة فيها تعارض بين القالب وعقود System Check.
- يتضمن تنظيف R3 المرئي المطلوب.

## التركيب
ارفع ZIP فقط من مركز تحديثات النظام. بعد النجاح اضغط إعادة تحميل الموقع.

## التحقق على PythonAnywhere
```bash
cd /home/Opalschool2016/opal_school
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test core.test_update120_unified_ux_contract core.test_update131_5_dashboard_layout_contract
python manage.py collectstatic --noinput
```

النتيجة المطلوبة: لا أخطاء SystemCheck، ولا هجرات جديدة، ونجاح الاختبارات.
