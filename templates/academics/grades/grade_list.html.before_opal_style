{% extends "base/base.html" %}

{% block page_title %}الصفوف الدراسية{% endblock %}
{% block page_subtitle %}إدارة الصفوف الدراسية ضمن نظام OPAL ERP{% endblock %}

{% block content %}
<div class="opal-card p-4 mb-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h3 class="mb-1">الصفوف الدراسية</h3>
            <p class="text-muted mb-0">إضافة وتعديل وتنظيم الصفوف حسب ترتيبها الأكاديمي</p>
        </div>
        <a href="{% url 'academics:grade_create' %}" class="btn btn-primary">
            + إضافة صف
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover align-middle">
            <thead>
                <tr>
                    <th>اسم الصف</th>
                    <th>الترتيب</th>
                    <th>الحالة</th>
                    <th class="text-center">الإجراءات</th>
                </tr>
            </thead>
            <tbody>
                {% for grade in grades %}
                <tr>
                    <td>{{ grade.name }}</td>
                    <td>{{ grade.order }}</td>
                    <td>
                        {% if grade.is_active %}
                            <span class="badge bg-success">فعال</span>
                        {% else %}
                            <span class="badge bg-secondary">غير فعال</span>
                        {% endif %}
                    </td>
                    <td class="text-center">
                        <a href="{% url 'academics:grade_update' grade.id %}" class="btn btn-sm btn-warning">تعديل</a>
                        <a href="{% url 'academics:grade_delete' grade.id %}" class="btn btn-sm btn-danger"
                           onclick="return confirm('هل أنت متأكد من حذف هذا الصف؟')">حذف</a>
                    </td>
                </tr>
                {% empty %}
                <tr>
                    <td colspan="4" class="text-center text-muted py-4">لا توجد صفوف بعد</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
