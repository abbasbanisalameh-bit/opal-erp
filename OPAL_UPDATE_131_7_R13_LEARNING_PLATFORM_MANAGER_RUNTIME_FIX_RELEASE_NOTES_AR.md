# ملاحظات إصدار OPAL Update 131.7 R13

## النتيجة

إصلاح خطأ HTTP 500 الذي ظهر عند فتح لوحة إدارة منصة أوبال التعليمية بعد تركيب R12.

## السبب الجذري

كانت `learning_platform.views.manager_dashboard` تستدعي `LearningAccount.objects` دون أن يكون `LearningAccount` مستوردًا من `learning_platform.models`.

## التعديل

- إضافة الاستيراد المفقود فقط.
- إضافة عقد مصدر يمنع تكرار المشكلة.
- الاعتماد على اختبار العرض التشغيلي الموجود الذي يطلب `/learning/manage/` ويتوقع HTTP 200.
- لا تغيير في قاعدة البيانات أو الجلسات أو الصلاحيات أو فصل حسابات المنصة عن OPAL ERP.
