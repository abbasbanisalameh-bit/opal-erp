# OPAL Update 112.0 — الإزالة الدائمة لنظام طلبات القبول القديم

## نقطة الأساس

هذه حزمة كاملة مبنية مباشرة فوق **OPAL Update 111.0**، وتتضمن جميع إصلاحاته السابقة، ومنها إصلاح طبقات الإشعارات وإصلاح مركز إدخال وتصفير البيانات.

## سبب التحديث

كانت صفحات المرشحين قد أزيلت سابقًا، لكن النموذج القديم `admissions.AdmissionApplication` بقي مسجلًا داخل وقت تشغيل Django وجدول قاعدة البيانات، مع استدعاءات قديمة في الإدارة ومركز التصفير. هذا يخالف قرار OPAL بأن يبدأ القبول من **التسجيل المباشر الذكي** فقط، ويترك نظامًا موازيًا غير مستخدم.

## ما تم تنفيذه

1. حذف نموذج `AdmissionApplication` نهائيًا من `admissions/models.py`.
2. إضافة Migration رسمية تحذف جدول النموذج القديم من قاعدة البيانات، دون `--fake` ودون تعديل يدوي لقاعدة البيانات.
3. إزالة تسجيل النموذج من Django Admin.
4. إزالة جميع استدعاءاته من النماذج والواجهات ومركز تصفير البيانات والاختبارات.
5. جعل شاشة التسجيل تعرض `StudentRegistration` فقط بوصفه سجل عمليات التسجيل الرسمي.
6. إضافة فحص معماري داخل التدقيق النهائي يمنع إعادة النموذج القديم مستقبلًا.
7. إضافة اختبارات تعاقدية تؤكد غياب النموذج واعتماد التسجيل المباشر فقط.

## المسار الرسمي بعد التحديث

- إدخال طالب جديد: `admissions:direct_registration`.
- سجل التسجيلات: `StudentRegistration`.
- الطالب الرسمي الوحيد: `students.Student`.
- لا توجد مرحلة مرشح أو طلب قبول موازية.

## سلامة البيانات

Migration تحذف فقط الجدول القديم الخاص بطلبات القبول. لا تمس الطلاب الرسميين أو التسجيلات أو الفواتير أو الدفعات أو الإيصالات أو أولياء الأمور.

## الترحيلات

يضيف التحديث:

- `admissions.0013_delete_admissionapplication`

ينفذها مركز التحديث تلقائيًا ضمن مسار الفحص والترحيل الآمن.
