# OPAL Update 131.7 R35.2 — Manager Mobile SSO

R35.2 يكمل تطبيق Android بإضافة وضع الإدارة باستخدام هوية OPAL ERP نفسها.

## الوظائف
- تسجيل الإدارة من `auth/school-login/` بنفس بيانات OPAL.
- لا يوجد حساب/كلمة مرور مدير منفصلة داخل `LearningAccount`.
- رمز إدارة `olm_` مستقل عن رموز المتعلمين والمعلمين `olp_`، قصير العمر، مخزن كبصمة Hash وقابل للإلغاء.
- صلاحية الإدارة يعاد فحصها في كل طلب باستخدام قاعدة OPAL الحالية نفسها.
- `/me/` و`/auth/logout/` يدعمان رمز الإدارة دون توسيع صلاحيات واجهات المتعلم/المعلم.
- واجهات موبايل إدارية للملخص، الحسابات، المواد، الدورات، نشر/أرشفة الدورات، البطاقات، الجاهزية وإتاحة المدرسة.
- إنشاء البطاقات من التطبيق يعيد استخدام `LearningSubscriptionBatchForm` والمولد العشوائي الآمن المركزي R33.
- التحكم بالإتاحة يحافظ على الأولوية الحالية: طالب ← صف ← إعداد عام.
- Flutter version: `1.2.0+37`.

## عدم التغيير
- لا تغيير على `students.Student`.
- لا تغيير على تسجيل ولي الأمر أو المعلم.
- لا تغيير على سياسة بطاقات R33.
- لا تغيير على شرط النسخة الاحتياطية.
- لا أسرار أو مفاتيح توقيع داخل التطبيق.
