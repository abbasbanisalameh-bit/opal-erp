# تقرير تحقق OPAL Update 131.7 R12

## النتيجة المحلية

- فحص تركيب جميع ملفات Python عبر `compileall`: ناجح.
- بوابة المصدر `tools/validate_opal_update_131_source.py`: ناجحة، **224/224** دون أخطاء أو تحذيرات.
- لا توجد هجرة قاعدة بيانات جديدة في R12؛ التحديث يستخدم جداول منصة R11 نفسها.
- تم تحديث عقد المدخل الموحد بحيث يكون رابط المنصة مدخلًا إداريًا رسميًا واحدًا.
- تم إضافة اختبارات تكامل للمصادقة عبر OPAL ERP وفصل جلسة مستخدمي المنصة.

## قيد بيئة الفحص

لم تتوفر حزمة Django في بيئة البناء المحلية، لذلك لم تُشغّل اختبارات Django runtime هنا. يجب تشغيل أوامر `check` والاختبارات و`makemigrations --check --dry-run` على البيئة الافتراضية الفعلية في PythonAnywhere قبل Reload، وفق دليل التركيب المرفق.

## معايير القبول على الخادم

1. نجاح `python manage.py check`.
2. ظهور `No changes detected` من فحص الهجرات.
3. نجاح اختبارات `learning_platform` وعقد R12 والتنقل.
4. ظهور رابط **منصة أوبال التعليمية** لمدير OPAL ERP.
5. فتح `/learning/manage/` بجلسة المدير دون إنشاء `LearningAccount`.
6. بقاء `/learning/login/` و`/learning/register/` مستقلين لبقية المستخدمين.
