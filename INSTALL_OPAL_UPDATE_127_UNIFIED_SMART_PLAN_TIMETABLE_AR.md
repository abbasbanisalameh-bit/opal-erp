# تثبيت OPAL Update 127 — الخطة والجدول الذكي الموحد

## قبل التثبيت

- الحزمة مبنية مباشرة على OPAL Update 126.
- لا تصفّر البيانات التجريبية.
- ثبّت الحزمة من مركز تحديثات OPAL المعتاد؛ المركز يتولى النسخة الاحتياطية والفحص والترحيلات وتجميع الملفات وإعادة التحميل والتراجع عند الفشل.

## بعد التثبيت

تحقق من نجاح الخطوات الآتية في سجل مركز التحديثات:

```bash
python manage.py check
python manage.py migrate
python manage.py collectstatic --noinput
```

وللتدقيق اليدوي عند الحاجة:

```bash
cd /home/Opalschool2016/opal_school
python manage.py check
python manage.py makemigrations --check
python manage.py migrate
python manage.py audit_final_stability --skip-templates
```

## إعداد النظام

1. افتح الخطة الدراسية الحالية، واضبط عدد حصص كل مادة وصف.
2. افتح ملف كل معلم ثم نموذج التكليف الحالي، واضبط النصاب وسياسة الفراغ وأكمل التكليفات.
3. افتح أوقات الحصص الحالية، وتأكد من ترتيب الحصص الأساسية.
4. افتح أوقات الحصص والاستراحات الحالية:
   - اجعل العطلة الجمعة والسبت.
   - أضف الاستراحات ومددها.
   - اختر مجموعة الشعب لكل استراحة.
   - اختر وقتًا ثابتًا أو توزيعًا ذكيًا.
5. افتح منشئ الجدول الذكي الحالي، واختر العام الدراسي ثم اضغط «اقتراح جدول».
6. عالج أي مشكلة جاهزية تظهر قبل الاعتماد.
7. راجع الاقتراح أو اطلب بديلًا أو عدل يوم/حصة ثم اضغط «فحص التعديلات».
8. اعتمد الجدول فقط عندما تصبح كل حالات الجاهزية سليمة.

## ملاحظات مهمة

- الاستراحة الذكية يجب أن تقع بكامل مدتها بعد ساعتين من بداية اليوم وقبل ساعتين من نهايته.
- توجد ساحة واحدة؛ لذلك لا يسمح بتداخل الاستراحات.
- النظام لا ينقل أي شعبة من مجموعة استراحة إلى أخرى.
- لا يسمح بتجاوز نصاب المعلم أو فرض فراغ غير قابل للتطبيق.
- الجدول الحالي يبقى فعالًا حتى نجاح الاعتماد الكامل.
