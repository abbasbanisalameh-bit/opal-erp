# تركيب OPAL Update 109.0 — الاستقرار وتبسيط الواجهة

## استخدم هذه الحزمة فقط

Update 109 حزمة موحدة كاملة وتشمل Update 108. لا تحتاج إلى تركيب Update 108 قبلها.

## الخطوات البسيطة

1. سجّل الدخول بحساب **مدير النظام الأعلى**.
2. افتح **إعدادات النظام ← مركز تحديثات النظام**.
3. أنشئ نسخة كود من الوضع الحالي.
4. ارفع ملف ZIP الخاص بـ Update 109.
5. من قائمة النسخ، اختر **استعادة** واكتب كلمة `استعادة`.
6. انتظر حتى ينتهي المركز من الفحص والترحيل وتجميع الملفات الثابتة.
7. استخدم زر **إعادة تحميل الموقع** من مركز التحديث عند نجاح العملية.

لا تحذف قاعدة البيانات، ولا تستخدم `--fake`، ولا تركّب Update 108 بعدها.

## فحص سريع بعد التركيب

- افتح **رسوم الطلاب**: يجب أن يظهر المدفوع والمتبقي والحالة بشكل طبيعي وبسرعة أفضل.
- افتح **الأقساط**: يجب أن تعرض الحالة الصحيحة من دون أن ينشئ فتح الصفحة أي تعديل في البيانات.
- بحساب موظف عادي: يجب ألا يفتح مركز التطوير أو رابط Gantt.
- بحساب مدير النظام الأعلى: يظل مركز التحديث ومركز التطوير متاحين.

## قبل إدخال البيانات الحقيقية

اضبط `OPAL_SECRET_KEY` في بيئة الخادم، وتأكد من وجود حساب مدير نظام فعّال، ثم شغّل:

```bash
python manage.py audit_production_readiness --strict
```
