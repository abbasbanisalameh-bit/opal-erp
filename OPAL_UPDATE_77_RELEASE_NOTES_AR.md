# تحديث OPAL رقم 77 — النسخ الاحتياطي وبروفة الاستعادة

تحديث تشغيلي آمن فوق الإصدار 76 يضيف أمر تدقيق لإنشاء نسخة قاعدة بيانات معزولة والتحقق من سلامتها وتجربة استعادتها في مجلد مؤقت دون المساس بقاعدة البيانات الحية.

## المضاف
- `audit_backup_recovery`.
- فحص سلامة نسخة SQLite عبر `PRAGMA integrity_check`.
- بروفة استعادة في نسخة مؤقتة معزولة.
- بصمة SHA-256 للنسخة الناتجة.
- فحص حالة حفظ الكود في Git ووجود origin عند توفر `.git`.
- تقرير JSON ودليل تشغيل عربي.

## غير المشمول
- حذف البيانات.
- الاستعادة فوق قاعدة البيانات الحية.
- تعديل Models أو migrations أو الصلاحيات أو المسارات.
