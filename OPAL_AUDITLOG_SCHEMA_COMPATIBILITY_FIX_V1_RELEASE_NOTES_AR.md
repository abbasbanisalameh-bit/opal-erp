# OPAL AUDITLOG SCHEMA COMPATIBILITY FIX V1

## الهدف
إصلاح خطأ 500 الناتج عن عدم توافق كود النسخة المستعادة مع مخطط قاعدة البيانات بعد تطبيق ترحيلات التحديث السابق.

## نطاق التحديث
- إعادة تعريف حقول سجل التدقيق الموجودة فعليًا في قاعدة البيانات:
  - school
  - branch
  - request_id
  - result
- إضافة قيمة افتراضية آمنة للحقل request_id.
- إعادة ملفي الترحيل core 0007 و0008 إلى شجرة الترحيلات حتى تتطابق حالة Django مع قاعدة البيانات الحالية.
- إضافة اختبار رجعي لإنشاء سجل التدقيق وتشغيل فحص سلامة البيانات المرتبط بالمدرسة.

## خارج النطاق
- لا توجد صفحات جديدة.
- لا توجد تغييرات في الصلاحيات.
- لا توجد تغييرات في الرسوم أو الطلاب أو التسجيل.
- لا يتم حذف أو تعديل البيانات الحالية.
