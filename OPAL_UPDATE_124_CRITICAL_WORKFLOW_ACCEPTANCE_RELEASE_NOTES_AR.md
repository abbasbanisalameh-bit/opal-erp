# OPAL Update 124 — بوابة اعتماد المسارات التشغيلية الحرجة

## أساس التحديث
بُني هذا الإصدار مباشرة على **OPAL Update 123**. لا يضيف نظامًا أو صفحة موازية، بل يثبت ويختبر المسارات التي عولجت في الإصدار السابق قبل الانتقال إلى البيانات الحقيقية.

## بوابة تلقائية داخل `manage.py check`
أصبحت الفحوص الرسمية تمنع أي تحديث لاحق يعيد كسر أحد العقود التالية:

- فتح لوحة وحساب ولي الأمر دون خلط حقل الصورة مع نموذج بيانات الأسرة.
- بقاء بطاقة **الشكاوى والاقتراحات** وزر الإرسال الصريح متاحين لولي الأمر والمعلم.
- استخدام صفحة واحدة موحدة للإرسال والمتابعة والرد مع حماية CSRF وربط الرسالة بالحساب الحالي.
- منع تجاوز دورة إنهاء الخدمة من نموذج تعديل المعلم العام.
- تنفيذ إنهاء الخدمة داخل معاملة واحدة مع قفل سجل المعلم، وإيقاف الحساب والتكليفات والحصص المفتوحة، وإصدار الكتاب الرسمي.
- إعادة تفعيل الحساب دون إعادة التكليفات أو الجدول القديم تلقائيًا.
- بقاء إيصال التسجيل وإيصال دفعة الرسوم مستقلين عن القالب العام، وبنسختين على صفحة A4 أفقية واحدة.
- بقاء مولد PDF المالي القديم أيضًا على صفحة واحدة ذات نسختين.

## تدقيق بيانات التشغيل
أضيف الأمر:

```bash
python manage.py audit_critical_workflows
```

وهو فحص للقراءة فقط يدقق:

- ملفات أولياء الأمور المدمجة أو المرتبطة بحسابات دخول غير فعالة.
- المعلم النشط الذي ما زالت عليه بيانات انتهاء خدمة.
- المعلم منتهي الخدمة وحسابه ما زال فعالًا.
- وجود تكليفات أو حصص فعالة لمعلم منتهي الخدمة في عام مفتوح.
- وجود كتاب إنهاء خدمة للمعلمين الذين أُنهيت خدمتهم بعد اعتماد المسار الرسمي.
- السجلات القديمة غير النشطة التي لا تحمل تاريخ إنهاء خدمة، وتظهر كتحذير للمراجعة دون حذفها.

ويدعم حفظ تقرير JSON:

```bash
python manage.py audit_critical_workflows --format json --output opal_private_backups/critical_workflows.json
```

كما يمكن اعتبار التحذيرات مانعة للاعتماد:

```bash
python manage.py audit_critical_workflows --strict-warnings
```

## تحسين ملف المعلم
- عند كون المعلم غير نشط ووجود كتاب إنهاء خدمة، يظهر زر مباشر **فتح آخر كتاب إنهاء خدمة** داخل ملف المعلم.
- أُعيد استخدام استعلام الوثائق نفسه دون إنشاء صفحة أو استعلام متكرر لكل وثيقة.

## الدمج مع بوابات الجاهزية
- دمج العقود الجديدة داخل `python manage.py check` برمز `opal.E124`.
- دمجها داخل `python manage.py audit_final_stability`.
- دمج فحص العقود وبيانات دورة الخدمة داخل `python manage.py verify_core_readiness`.

## السلامة
- لا توجد Migration جديدة.
- لا يوجد حذف أو تعديل لبيانات المدرسة.
- لا تتغير الرسوم أو الدفعات أو الحضور أو الامتحانات أو أوزان التقييم.
- بقي `students.Student` نموذج الطالب الرسمي الوحيد.
- جميع عمليات التدقيق الجديدة للقراءة فقط.
