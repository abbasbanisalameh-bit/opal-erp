# OPAL Update 122 — بوابة الاستقرار النهائي وتوحيد سياق التشغيل

## أساس التحديث
بُني هذا الإصدار مباشرة على **OPAL Update 121**. وهو المرحلة النهائية من خطة تحسين السرعة وتجربة الاستخدام، ويركز على تثبيت قشرة النظام العامة ومنع عودة التكرار المعماري أو أخطاء الروابط والثيم بعد التحديثات اللاحقة.

## سياق تشغيل واحد لكل طلب
- إضافة طبقة مركزية تقرأ ملف المستخدم والمدرسة والسياق الأكاديمي مرة واحدة لكل طلب HTTP.
- إعادة استخدام المدرسة نفسها في شريط الهوية وحالة الجدول الحي بدل تنفيذ استعلام مستقل في كل Context Processor.
- الحفاظ على قاعدة الاختيار الحالية: مدرسة المستخدم أولًا، ثم المدرسة الفعالة، دون إنشاء سجل أو تغيير البيانات.
- لا يتغير منطق العام الدراسي أو الفصل أو الصلاحيات أو حالة المعلمين المشغولين والمتفرغين.

## بوابة استقرار تلقائية داخل `manage.py check`
أصبح الفحص الرسمي يرفض التحديث إذا اكتشف أيًا من الآتي:

- Context Processor مكرر أو مفقود.
- تشغيل حالة الجدول قبل تحميل هوية المدرسة المركزية.
- تكرار تحميل ملف CSS أو JavaScript نفسه في القالب الأساسي.
- غياب ملفات OPAL الأساسية أو تحميل نظام الثيم بترتيب خاطئ.
- عملية في دليل العمليات تشير إلى رابط غير موجود.
- عنصر مكرر أو غير معروف في القوائم الجانبية حسب الدور.
- بوابة معلومات رسمية لا يمكن فتح مسارها.

جميع هذه الفحوص **للقراءة فقط** ولا تعدّل قاعدة البيانات.

## أمر التدقيق النهائي
تمت إضافة الأمر:

```bash
python manage.py audit_final_stability
```

ويدقق:
- صياغة ملفات Python.
- نموذج الطالب الرسمي الوحيد `students.Student`.
- عدم عودة نظام طلبات القبول القديم.
- أسماء الروابط والقوالب وملفات الهوية.
- قشرة التشغيل والقوائم وبوابات المعلومات.

ويمكن حفظ تقرير JSON:

```bash
python manage.py audit_final_stability --format json --output opal_private_backups/final_stability.json
```

## تثبيت هوية الإصدار
- تحديث الإصدار الرسمي إلى `122.0`.
- تحديث مفتاح ملفات الواجهة لمنع استخدام نسخة متصفح قديمة بعد التركيب.
- دمج فحص القشرة داخل أمر الجاهزية الرسمي `verify_core_readiness`.

## السلامة
- لا توجد Migration جديدة.
- لا يوجد حذف أو تعديل لبيانات المدرسة.
- لا توجد صفحة أو وظيفة أو محرك موازٍ.
- لا تتغير أوزان TPI أو الحضور أو الرسوم أو الامتحانات.
- بقي `students.Student` نموذج الطالب الرسمي الوحيد.
