# تركيب تحديث OPAL 84 — استعادة الهوية البصرية

1. خذ نسخة احتياطية من قاعدة البيانات و`media`.
2. من **إدارة النظام ← تحديثات النظام** ارفع `OPAL_UPDATE_84_OPAL_IDENTITY_20260723.zip` ونفّذ التحديث.
3. أعد تحميل خدمة الويب من لوحة الاستضافة إذا كانت الاستضافة تتطلب ذلك، ثم حدّث المتصفح تحديثًا قويًا (`Ctrl+F5`) لمسح نسخة CSS المخزنة.

الحزمة لا تتضمن قاعدة البيانات أو `media` أو `.env` أو البيئة الافتراضية.

## التراجع

استخدم `OPAL_ROLLBACK_TO_83_20260723.zip` من شاشة التحديثات للرجوع إلى كود OPAL 83. لا توجد ترحيلات قاعدة بيانات في هذا التصحيح، لذلك لا يلزم تراجع قاعدة البيانات بسبب طبقة الهوية وحدها.
