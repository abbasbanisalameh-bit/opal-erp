# تثبيت OPAL Update 130.0

## الحزمة

ارفع ملف ZIP كما هو من مركز التحديثات داخل إعدادات النظام. لا تفك ضغطه ولا
تنسخ ملفاته يدويًا فوق المشروع.

هذه حزمة مصدر كاملة، لذلك تكفي وحدها للنسخة الحالية المرفقة، وتشمل التحديثات
128–129.2 وإصلاحات 130.0. لا تحتاج إلى تثبيت الحزم السابقة منفردة.

إذا كانت النسخة الحالية تعرض رسالة **تعذر حفظ ملف التحديث على الخادم** قبل
فحص الحزمة، شغّل أولًا الملف
`OPAL_ONE_TIME_UPLOAD_STORAGE_REPAIR_130.py` من Bash في PythonAnywhere، ثم اضغط
Reload من صفحة Web. هذا إصلاح تمهيدي لمركز التحديث وليس تحديثًا وظيفيًا ثانيًا؛
لا يحذف قاعدة البيانات أو الوسائط أو النسخ المكتملة.

## الخطوات

1. افتح **إعدادات النظام ← مركز التحديثات** بحساب مدير النظام.
2. اختر ملف `OPAL_UPDATE_130_0_PRODUCTION_STABILITY_AND_LIVE_HORIZONTAL_TIMETABLE_FULL.zip`.
3. نفذ الرفع والتطبيق، وانتظر رسالة النجاح النهائية من المركز.
4. أعد تحميل الصفحة تحميلًا كاملًا كي تُحمّل الأصول الجديدة.
5. تحقق من ظهور الإصدار `130.0`.
6. افتح بوابة الخطة والجدول وتأكد من ظهور الأيام في اليمين والحصص أفقيًا.

ينشئ مركز التحديثات نقطة أمان قبل التطبيق. الحزمة لا تحتوي قاعدة بيانات ولا
تستبدل بيانات المدرسة.

بعد التثبيت يحتفظ المركز تلقائيًا بآخر خمس نقاط أمان لقاعدة SQLite فقط، بعد
نجاح النقطة الجديدة والتحقق منها، حتى لا تمتلئ حصة التخزين بالنسخ المتكررة.

## تحقق سريع بعد التثبيت

- لا تتمدد الصفحة أفقيًا عند فتح القائمة على الهاتف.
- يظهر كل يوم مرة واحدة فقط في العمود الأيمن.
- تظهر حصص اليوم أفقيًا، وتتميز الحصة الجارية أثناء وقتها.
- تعمل لوحة القيادة وبوابة المعلم وولي الأمر والطالب.
- عند توفر الطرفية يمكن تشغيل:

```bash
python manage.py check
python manage.py migrate --noinput
```

## قبل فتح النظام للتشغيل الفعلي

اضبط القيم الآتية في بيئة الاستضافة، ولا تضع أسرارها داخل ملف التحديث:

- `OPAL_DEBUG=false`.
- `OPAL_SECRET_KEY` بقيمة إنتاجية طويلة وفريدة.
- `OPAL_ALLOWED_HOSTS` على نطاق الموقع الفعلي.
- `OPAL_CSRF_TRUSTED_ORIGINS` على رابط HTTPS الفعلي.
- `OPAL_SECURE_SSL_REDIRECT=true` بعد التأكد من عمل HTTPS والوكيل العكسي.
- `OPAL_HSTS_SECONDS` بعد التحقق من HTTPS؛ يبدأ عادة بقيمة قصيرة للاختبار ثم
  يرفع وفق سياسة الاستضافة.

وشغّل تجميع الملفات الثابتة، وتأكد من صلاحية مجلد الوسائط. أما رسوم الصفوف
وسعات الشعب ومربو الصفوف فتُستكمل من بيانات المدرسة قبل استقبال المستخدمين؛
لا تضيف الحزمة قيمًا تشغيلية افتراضية بدل القرار الإداري.

إذا أظهر مركز التحديثات رسالة فشل قبل بدء فك الحزمة، احتفظ بنص الرسالة كاملًا
ولا تعاود التطبيق مرات متتالية؛ تكون المشكلة حينها في صلاحية التخزين أو بيئة
التشغيل وليست في بيانات الحزمة.
