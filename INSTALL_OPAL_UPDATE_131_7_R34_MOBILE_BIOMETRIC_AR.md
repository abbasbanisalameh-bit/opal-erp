# OPAL Update 131.7 R34 — تطبيق الموبايل وتكامل بصمة المعلمين

## الهدف
يبني R34 فوق R33 مرحلتين مترابطتين دون إنشاء نظام طلاب أو حضور موازٍ:

1. **تطبيق OPAL Mobile — المرحلة الأولى**: دخول ولي الأمر والمعلم بنفس حساب OPAL ERP، اختيار الابن، استخدام هوية التعلم المستقلة لكل ابن، عرض الدورات والدروس والتقييمات والإشعارات والشهادات، تفعيل بطاقة الاشتراك، وتشغيل فيديو الدرس داخل التطبيق.
2. **بوابة بصمة المعلمين**: استقبال حركات أجهزة البصمة بصورة آمنة ومحايدة للشركة، ربط معرف الجهاز بالمعلم الرسمي، بناء ملخص يومي للمراجعة، ثم اعتماد الاستثناء في `timetable.TeacherAbsence` فقط بعد قرار الإدارة.

> R34 لا يلغي ولا يخفف شرط النسخة الاحتياطية في بوابة جاهزية منصة التعلم. إذا لم توجد نسخة متحققة حديثًا فسيبقى هذا هو المانع الرسمي الوحيد كما كان في R33.

## قواعد الأمان المعتمدة
- لا تخزن OPAL قوالب البصمة أو صورة الإصبع أو بيانات بيومترية خام؛ تبقى داخل الجهاز.
- يصل إلى OPAL فقط: معرف المستخدم داخل الجهاز، وقت الحركة، اتجاه دخول/خروج إن توفر، ومعرف الحدث.
- لكل جهاز رمز ربط سري مستقل، ولا يخزن الرمز الخام في قاعدة البيانات؛ يخزن SHA-256 فقط ويظهر الرمز للمدير مرة واحدة عند الإنشاء/التدوير.
- وصول حركة بصمة **لا ينشئ غيابًا أو خصمًا أو حضورًا رسميًا تلقائيًا**.
- الحالة الطبيعية لا تنشئ سجل حضور يومي جديد، حفاظًا على بنية OPAL المعتمدة القائمة على الاستثناءات فقط.
- الحالات المقترحة (تأخر/مغادرة مبكرة/غياب) لا تدخل `TeacherAbsence` إلا بعد اعتماد الإدارة.

## تركيب التحديث على PythonAnywhere
من مركز تحديثات النظام ارفع ZIP الخاص بـR34 وطبقه، ثم افتح Bash:

```bash
cd /home/Opalschool2016/opal_school
source venv/bin/activate

python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py migrate
```

المتوقع أن تطبق الهجرة الجديدة:

```text
timetable.0009_biometric_attendance_gateway
```

ثم شغّل اختبارات R34:

```bash
python manage.py test \
  core.test_update131_7_r34_mobile_biometric_contract \
  learning_platform.test_update131_7_r34_mobile_school_sso \
  timetable.test_update131_7_r34_biometric_gateway \
  --settings=config.settings_test_low_memory \
  --verbosity 1 \
  --noinput
```

المتوقع اكتشاف **10 اختبارات** وانتهاء التشغيل بـ:

```text
OK
```

ثم:

```bash
python manage.py collectstatic --clear --noinput
python manage.py check
```

وأخيرًا من صفحة **Web** في PythonAnywhere اضغط **Reload**.

## إعداد بوابة البصمة
بعد Reload:

1. افتح **الهيكل الأكاديمي → تكامل البصمة**.
2. أضف الجهاز: الاسم، رمز الجهاز، الشركة/النوع، الفرع والمنطقة الزمنية.
3. انسخ **رمز الربط** فور ظهوره؛ يظهر مرة واحدة فقط.
4. اربط `معرف المعلم داخل الجهاز` بالمعلم الرسمي في OPAL.
5. لا تعتمد أي ملخص حتى يبدأ الجهاز الفعلي بإرسال حركات صحيحة.

### لماذا يوجد جسر محلي؟
PythonAnywhere موجود على الإنترنت ولا يستطيع فتح اتصال مباشر إلى جهاز موجود داخل شبكة المدرسة المحلية. لذلك يعمل ملف الجسر على كمبيوتر داخل المدرسة، يقرأ الجهاز محليًا ثم يرسل الحركات **Outbound HTTPS** إلى OPAL.

ملفات الجسر:

```text
tools/biometric_bridge/opal_biometric_bridge.py
tools/biometric_bridge/README_AR.md
```

يدعم حاليًا:
- JSON عام لأي برنامج وسيط.
- ZKTeco عبر مكتبة `pyzk` عند تشغيل الخيار `--zkteco`.

إذا كان الجهاز من شركة أخرى أو يستخدم ADMS/Push/SDK خاصًا، يبقى Backend نفسه ويضاف Adapter للجهاز فقط.

## تطبيق الموبايل
المصدر موجود في:

```text
mobile/opal_learning_app/
```

### ما يعمل في R34
- دخول ولي الأمر بنفس اسم المستخدم وكلمة مرور OPAL ERP.
- ظهور الأبناء المسموح لهم بالمنصة فقط.
- اختيار الابن والتبديل بين الأبناء.
- Token تعلم مستقل لكل ابن؛ لا تختلط بيانات الإخوة.
- دخول المعلم بنفس حساب OPAL ERP.
- عرض دورات المعلم المرتبطة بحسابه.
- الدورات والدروس والتقدم والتقييمات والإشعارات والشهادات للمتعلم.
- تفعيل بطاقة اشتراك من التطبيق.
- تشغيل YouTube/Vimeo والفيديو المباشر داخل التطبيق بواسطة WebView.
- تخزين Tokens محليًا في `flutter_secure_storage`.

### ما لا يدعي R34 اكتماله بعد
- لا يوجد APK/IPA موقّع داخل حزمة السيرفر؛ PythonAnywhere لا يحتوي Flutter SDK ولا مفاتيح توقيع التطبيق.
- إنشاء/تحرير محتوى المعلم من واجهة Flutter الأصلية لم يكتمل في هذه المرحلة؛ الرفع والتحرير الكامل يبقيان عبر بوابة المعلم/المنصة على الويب.
- نشر التطبيق على Google Play / App Store مرحلة مستقلة بعد الاختبار والتوقيع.

### بناء Android/iOS على جهاز تطوير فيه Flutter
لأن الحزمة الحالية خفيفة ولا تحتوي مجلدات Android/iOS المولدة، نفذ مرة واحدة:

```bash
cd mobile/opal_learning_app
flutter create --platforms=android,ios .
flutter pub get
flutter analyze
flutter test
```

ثم بناء Android، مع استبدال النطاق بنطاق OPAL الحقيقي:

```bash
flutter build apk --release \
  --dart-define=OPAL_API_BASE_URL=https://YOUR-DOMAIN/learning/api/v1
```

## API الجديد
- `POST /learning/api/v1/auth/school-login/`
- `POST /learning/api/v1/subscription-cards/redeem/`
- `POST /timetable/biometric/api/v1/punches/`

## بعد نجاح التثبيت
أرسل نتيجة اختبارات R34. بعدها يتم:
1. ربط جهاز البصمة الحقيقي حسب الشركة والموديل وعنوان IP داخل المدرسة.
2. إنشاء أول Build فعلي لتطبيق Android واختباره على هاتف ولي أمر ومعلم.
