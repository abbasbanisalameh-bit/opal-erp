# تركيب OPAL Update 131.7 R5

يفصل هذا الإصدار رسوم السنة الدراسية الحالية عن متبقيات السنوات السابقة في التحصيل والتقارير وبوابة ولي الأمر وملف الطالب، مع إبقاء كل المبالغ في الفواتير والدفعات الرسمية نفسها.

## قبل التركيب

- أنشئ نسخة احتياطية كاملة من قاعدة البيانات وملفات المشروع.
- ثبّت الحزمة فوق مشروع OPAL الحالي مع الحفاظ على ملف `.env` وقاعدة البيانات وملفات `media`.
- لا تحتوي الحزمة على قاعدة بيانات أو ملفات تشغيل خاصة.

## ما يتغير

- شاشة **تسديد الرسوم** تقبل وتوزع الدفعات على فواتير السنة الحالية فقط.
- شاشة **متبقيات السنوات السابقة** تبقى المسار المخصص لتسديد الرصيد المتراكم، وتوزعه من أقدم فاتورة سابقة.
- تظهر القيم منفصلة: رسوم السنة الحالية، المدفوع الحالي، المتبقي الحالي، متبقيات السنوات السابقة، والإجمالي المطلوب.
- لا توجد هجرات جديدة ولا نموذج مالي موازٍ ولا تعديل تلقائي على الأرصدة.

## التحقق بعد التركيب

```bash
cd /home/Opalschool2016/opal_school
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test accounting.test_fee_year_separation core.test_update131_7_r5_fee_year_separation_contract accounting.test_previous_debt_services
python manage.py collectstatic --clear --noinput
```

بعد نجاح الأوامر، أعد تشغيل تطبيق الويب ثم اختبر إيصالًا للسنة الحالية وإيصالًا لمتبقيات السنوات السابقة على طالب تجريبي.
