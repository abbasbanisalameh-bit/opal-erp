# OPAL Update 131.7 R33 — ملاحظات الإصدار

## الهدف
إغلاق الفجوات الحقيقية التي ظهرت في تدقيق النسخة الحالية R32، من دون إعادة بناء وظائف مدرسية مكتملة أصلًا.

## التغييرات

### 1) فحص الفصل الدراسي
فحص تكامل العمليات يميز الآن بين:
- فترة دراسية داخل فصل مفتوح: يجب وجود فصل تشغيلي حالي.
- ما قبل بداية العام، عطلة منتصف العام، أو خارج حدود الفصل: غياب الفصل الحالي سلوك صحيح ولا يخفض صحة التكامل.

### 2) نموذج تحصيل المنصة
أضيف إعداد بيئي:

`OPAL_LEARNING_SUBSCRIPTION_SALES_MODE`

القيم:
- `cards` — الافتراضي، بطاقات اشتراك، صالح للإطلاق دون بوابة دفع خارجية.
- `manual` — تحصيل إداري داخلي/مدرسي.
- `payment` — دفع إلكتروني ويتطلب إعداد مزود خارجي.

### 3) بطاقات اشتراك غير قابلة للتنبؤ
المولد المركزي الجديد:
- يستخدم `secrets.choice`.
- أبجدية من 32 رمزًا واضحة للقراءة بدون `0/O` و`1/I`.
- 20 رمزًا عشوائيًا = قرابة 100 bit من العشوائية.
- فحص تفرد قبل الحفظ، مع `unique=True` في قاعدة البيانات كحاجز نهائي.
- لا يضم `student_number` أو `order.pk` أو أي عداد متسلسل.
- يطبق على إنشاء الدفعات من الإدارة، البطاقات الناتجة عن الدفع، والبيانات التجريبية.
- لا يغير رموز البطاقات القديمة.

### 4) تقرير الجاهزية
كل بند يوضح الآن نوعه:
- إلزامي.
- اختياري.
- قيد استضافة.

SQLite تبقى تحذير استضافة وليست خطأ برمجيًا.

### 5) قفل الاعتماديات
أضيف الأمر:

`python manage.py write_learning_dependency_lock`

وهو يكتب `requirements-lock-r20.txt` من `pip freeze` للبيئة الحية نفسها، حتى لا يتم تثبيت إصدارات مفترضة غير التي نجحت فيها الاختبارات.

## لم يتغير
- لا Migration جديدة.
- لا تعديل على `students.Student`.
- لا تغيير على الطلاب أو الرسوم أو الدفعات أو الجداول أو العلامات.
- لا إعادة ترقيم للبطاقات الموجودة.
- لا إلغاء لشرط النسخة الاحتياطية في بوابة الجاهزية.
