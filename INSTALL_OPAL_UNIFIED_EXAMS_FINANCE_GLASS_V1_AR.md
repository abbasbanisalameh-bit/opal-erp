# تركيب تحديث OPAL — الامتحانات والمالية والواجهة الموحدة

الإصدار: `OPAL_UNIFIED_EXAMS_FINANCE_GLASS_V1_20260719`

## قبل التركيب

1. احفظ نسخة كود احتياطية من **إعدادات النظام ← تحديثات النظام**.
2. تأكد أن النسخة الحالية تعمل وأن `python manage.py check` ناجح.
3. لا تحذف قاعدة البيانات ولا مجلد `media`.
4. يفضّل تنفيذ التحديث أولًا على نسخة تجريبية من الموقع.

## التركيب من مركز تحديثات OPAL

1. ادخل بحساب المدير الأعلى.
2. افتح **إعدادات النظام ← تحديثات النظام**.
3. ارفع ملف التحديث ZIP.
4. نفّذ الاستعادة وفق رسالة التأكيد.
5. انتظر تطبيق الترحيلات وتجميع الملفات الثابتة.
6. أعد تحميل تطبيق PythonAnywhere.
7. نفّذ تحديثًا كاملًا للمتصفح لمسح CSS وJavaScript القديمين.

## التركيب من الطرفية

```bash
cd ~/opal_school
source venv/bin/activate
python manage.py check
python manage.py migrate --noinput
python manage.py collectstatic --noinput
python manage.py makemigrations --check --dry-run
python manage.py audit_operation_flow --allow-issues
```

ثم أعد تحميل الموقع من PythonAnywhere.

## جدولة الإغلاق الشهري

أنشئ مهمة مجدولة يومية في PythonAnywhere تشغّل:

```bash
cd ~/opal_school && source venv/bin/activate && python manage.py close_monthly_finance
```

الأمر لا يغلق شيئًا إلا إذا كان اليوم هو آخر يوم فعلي من الشهر، ولا يعيد إغلاق شهر مغلق.

## اختبار القبول بعد التركيب

1. افتح **الامتحانات** وتأكد أن العام والفصل الحاليين محددان تلقائيًا.
2. اختر الصف والشعبة والمادة وتأكد من ظهور المعلم المكلف.
3. أنشئ الامتحان الأول بحساب المدير.
4. ادخل بحساب المعلم، املأ علامات جميع الطلبة، ثم اضغط **حفظ وإرسال إلى الإدارة**.
5. بحساب المدير افتح الامتحان المرسل واعتمده.
6. تأكد أن النتيجة ظهرت في بوابة ولي الأمر وأن سجلات OpenEMIS أصبحت في حالة انتظار إذا كانت الوحدة مفعلة.
7. افتح **المصروفات والمقصف** وسجّل مصروفًا برقم فاتورة ومبيعات مقصف بفاتورة.
8. افتح **الكشف الشهري** وتأكد من الوارد والمصروف والربح والترحيل.
9. افتح **إعدادات النظام** وتأكد من الأقسام الأفقية وإعدادات التسجيل والهيكل الدراسي.
10. اختبر الإشعارات والبحث الفوري وزر العودة ومسار الصفحة على الهاتف والكمبيوتر.

## إعدادات الإنتاج المطلوبة

اضبط على الخادم على الأقل:

```bash
OPAL_SECRET_KEY=<مفتاح عشوائي طويل>
OPAL_DEBUG=False
OPAL_ALLOWED_HOSTS=opalschool2016.pythonanywhere.com
```

وفعّل تحويل HTTPS وHSTS من إعدادات الخادم بعد التأكد من عمل HTTPS بصورة صحيحة.

## الرجوع

استخدم نسخة الكود الاحتياطية التي أُنشئت قبل التحديث. لأن الإصدار يحتوي ترحيلات قاعدة بيانات جديدة، يجب أن يكون الرجوع مخططًا: استعادة الكود وقاعدة البيانات من النسخة المتوافقة نفسها، لا استعادة الكود القديم فوق مخطط قاعدة جديد بصورة عشوائية.
