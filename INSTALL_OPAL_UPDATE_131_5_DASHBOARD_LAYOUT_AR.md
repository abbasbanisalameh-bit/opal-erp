# تثبيت OPAL Update 131.5 R2 — مصمم لوحة المدير المبسّط

## الغرض

يستبدل هذا التحديث واجهة التخصيص السابقة المعقدة بأداة مركزية واحدة أعلى لوحة المدير. يمكن تحديد عنصر واحد أو عدة عناصر أو جميع العناصر، ثم تطبيق الحجم والارتفاع والشكل والحواف والإظهار عليها دفعة واحدة.

## قبل التثبيت

- النسخة المستهدفة: OPAL Update 131.4.
- لا تحذف هجرات `accounts` يدويًا.
- مركز التحديثات ينشئ نسخة أمان من الكود وقاعدة SQLite قبل التطبيق.
- الحزمة لا تحتوي قاعدة بيانات أو `media` أو `.env` أو بيئة افتراضية.

## التركيب

ارفع ملف ZIP من مركز تحديثات النظام، وانتظر نجاح الفحص والهجرة، ثم اضغط **إعادة تحميل الموقع** مرة واحدة.

## أوامر التحقق على PythonAnywhere

```bash
cd /home/Opalschool2016/opal_school
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py showmigrations accounts
python manage.py test core.test_update131_5_dashboard_layout_contract
python manage.py collectstatic --noinput
```

النتيجة المطلوبة تشمل:

```text
System check identified no issues
No changes detected
[X] 0002_userprofile_dashboard_layout
OK
```

## الاستخدام

1. افتح لوحة المدير واضغط **تخصيص اللوحة**.
2. حدّد عنصرًا بالنقر عليه، أو استخدم مربعات التحديد، أو اضغط **تحديد الكل**.
3. اختر العرض والارتفاع والشكل والحواف من الأداة المركزية ثم اضغط **تطبيق على المحدد**.
4. استخدم **إظهار** أو **إخفاء** للعناصر المحددة.
5. اسحب المقبض الصغير لترتيب عنصر أو مجموعة محددة، أو استخدم **تقديم/تأخير**.
6. يمكن تطبيق تخطيط جاهز: مضغوط، متوازن، أو موسع.
7. اضغط **حفظ** لتثبيت التفضيلات في حساب المدير.

## التوافق مع أثر 131.5 السابق

التخطيط القديم من واجهة الأدوات المتكررة يحمل المخطط رقم 1. هذا التحديث يستخدم المخطط رقم 2 ويعيد أي تخطيط قديم تلقائيًا إلى التخطيط المتوازن، حتى لا تعود الترتيبات التي استعاد المستخدم 131.4 للتخلص منها.

هجرة `accounts.0002` تتحقق من وجود العمود قبل إضافته، لذلك تتعامل بأمان مع حالة نادرة بقي فيها عمود `dashboard_layout` بعد استعادة كود سابقة.
