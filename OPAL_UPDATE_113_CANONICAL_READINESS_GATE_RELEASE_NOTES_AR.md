# OPAL Update 113.0 — توحيد بوابة الجاهزية التشغيلية

## نقطة الأساس

هذه حزمة كاملة مبنية مباشرة فوق **OPAL Update 112.0**، وتشمل جميع إصلاحاته السابقة، ومنها إزالة نظام طلبات القبول القديم واعتماد التسجيل المباشر الذكي فقط.

## سبب التحديث

كان داخل المشروع أمران منفصلان يفحصان جاهزية التشغيل الفعلي:

- `verify_core_readiness`
- `audit_production_readiness`

وكان كل أمر يحتوي منطقًا مختلفًا ونتائج مختلفة، بما في ذلك رقم إصدار تاريخي قديم داخل أحد التقارير. هذا يخالف قاعدة OPAL: **مصدر واحد للحقيقة ومحرك واحد لكل عملية**، وقد يؤدي إلى اعتماد الموقع من فحص ورفضه من فحص آخر.

## ما تم تنفيذه

1. اعتماد `verify_core_readiness` بوصفه محرك الجاهزية الرسمي والوحيد.
2. تحويل `audit_production_readiness` إلى اسم توافق تاريخي يمرر الطلب مباشرة إلى المحرك الرسمي دون أي حسابات أو استعلامات موازية.
3. الحفاظ على الخيارات التاريخية للأمر القديم، ومنها `--strict` و`--output`، مع تحويلها إلى الخيارات الرسمية المقابلة.
4. إضافة خيار `--output` إلى المحرك الرسمي لحفظ تقرير JSON قابل للأرشفة.
5. توسيع الفحص الرسمي ليشمل:
   - ضبط `ALLOWED_HOSTS` ومنع القيمة العامة `*`.
   - وجود ملفات OPAL الأساسية للواجهة والتشغيل.
   - وجود مجلدي الوسائط و`static` وقابليتهما للكتابة.
6. إضافة بيانات تعريف واضحة للتقرير بالإصدار `113.0` وعقد سلامة يؤكد أن الفحص لا يحذف البيانات ولا يعدل قاعدة البيانات.
7. إضافة اختبارات تعاقدية تمنع إعادة إنشاء محرك جاهزية موازٍ مستقبلًا.

## الاستخدام الرسمي

الفحص الأولي لنسخة جديدة:

```bash
python manage.py verify_core_readiness --allow-empty
```

الفحص الصارم قبل التشغيل الفعلي:

```bash
python manage.py verify_core_readiness --strict-warnings --output reports/production-readiness.json
```

يبقى الأمر التالي صالحًا فقط للتوافق مع النصوص القديمة، لكنه ينفذ المحرك نفسه:

```bash
python manage.py audit_production_readiness --strict --output reports/production-readiness.json
```

## سلامة البيانات

- لا توجد Migration جديدة.
- لا حذف أو تعديل لأي سجل مدرسي.
- لا تغيير على `students.Student` أو التسجيل أو الرسوم أو الحضور أو العلامات.
- لا إضافة لأي نظام موازٍ أو صفحة جديدة.
