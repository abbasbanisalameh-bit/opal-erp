# OPAL Update 118 — خط أساس الأداء والتحسين السريع للوحة الإدارة

## الهدف
هذا التحديث هو المرحلة الأولى من خطة تسريع OPAL وتحسين تجربة المستخدم. يضع قياسًا موحدًا قبل/بعد، ثم يطبق تحسينًا آمنًا ومباشرًا على لوحة الإدارة دون تغيير الأرقام أو الوظائف.

## أداة قياس الأداء الرسمية
أضيف الأمر:

```bash
python manage.py audit_runtime_performance
```

ويقوم للقراءة فقط بقياس الصفحات الإدارية الأساسية، ويعرض لكل صفحة:

- الزمن الكلي للطلب بعد طلب تهيئة اختياري.
- عدد استعلامات قاعدة البيانات.
- مجموع زمن قاعدة البيانات.
- حجم الاستجابة.
- الاستعلامات المتكررة بعد إخفاء قيم البيانات الحقيقية.
- الاشتباه في نمط `N+1`.
- أبطأ الاستعلامات.
- حالة الصفحة ورمز HTTP.
- تجاوز ميزانية الصفحة المحددة من عدمه.

تتكرر كل عملية قياس ثلاث مرات افتراضيًا ويعتمد التقرير القيمة الوسيطة، لا أسرع محاولة فقط.

## الصفحات الافتراضية
يغطي الفحص الافتراضي:

- لوحة الإدارة التنفيذية.
- لوحة الرسوم والدفعات.
- قائمة الرسوم والفواتير.
- لوحة الحضور.
- تقرير الحضور.
- قائمة الطلبة.
- قائمة المعلمين.
- الشؤون الأكاديمية.
- الجدول المدرسي.
- مركز التقارير.

يمكن قياس صفحة واحدة أو مسار داخلي محدد دون تغيير البيانات.

## تقارير قبل/بعد
- يحفظ الأمر تقرير JSON قابلًا للمقارنة وتقرير Markdown مقروءًا.
- تحفظ التقارير افتراضيًا داخل مساحة النظام الخاصة المستخدمة للنسخ، وليس داخل الملفات العامة للموقع.
- يمكن تمرير تقرير سابق عبر `--compare-to` لإظهار فرق الزمن وعدد الاستعلامات لكل صفحة.
- لا تُحفظ القيم الفعلية الموجودة داخل SQL؛ تُستبدل القيم ببصمات آمنة قبل كتابة التقرير.

## التحسين السريع للوحة الإدارة
كانت لوحة الإدارة تحسب في كل فتح بعض البيانات التي أزيل عرضها أو لا تحتاجها الصفحة المدمجة، ومنها:

- عدد امتحانات النظام بعد إزالة بطاقته.
- عدد الوثائق المصدرة بعد إزالة بطاقتها.
- قائمة المتابعة المالية التفصيلية غير المعروضة.
- قائمة مخاطر الحضور التفصيلية غير المعروضة في الصفحة الرئيسية.

أصبحت الصفحة الرئيسية تطلب نسخة مدمجة من المحرك المركزي وتتجاوز هذه الاستعلامات فقط. بقي المحرك الكامل هو المصدر الرسمي نفسه، وتبقى البيانات متاحة لتصدير الملخص وصفحة تفاصيل الحضور والتكاملات القديمة.

تم تشديد حد اختبار استعلامات لوحة الإدارة من 29 إلى 26 استعلامًا في قاعدة الاختبار الفارغة.

## السلامة
- جميع طلبات أداة الأداء من نوع `GET` فقط.
- لا يوجد إنشاء أو تعديل أو حذف لبيانات المدرسة.
- لا توجد Migration جديدة.
- لا يوجد كاش جديد قد يعرض أرقامًا قديمة.
- لم تتغير الصلاحيات أو المسارات أو القوالب المرئية.
- بقي `students.Student` نموذج الطالب الرسمي الوحيد.
