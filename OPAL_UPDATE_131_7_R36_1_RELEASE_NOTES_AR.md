# OPAL Update 131.7 R36.1 — Flutter Brand Analyze Const Hotfix

- إصلاح فشل خطوة `Analyze` في GitHub Actions بعد R36.
- السبب: `CardThemeData` كان قابلاً للإنشاء كـ `const`، ولذلك أبلغ Flutter عن ست ملاحظات `prefer_const_constructors` وانتهت خطوة التحليل برمز خروج 1.
- الإصلاح: تحويل `cardTheme` إلى سياق ثابت `const`، ما يغطي المنشئات المتداخلة في نفس الكتلة.
- لا تغيير على تجربة R36 البصرية أو تسجيل الدخول أو البيانات أو الصلاحيات.
- يبقى إصدار تطبيق المنصة `1.3.0+39` لأن APK الخاص بـ R36 لم ينجح بناؤه أصلًا.
